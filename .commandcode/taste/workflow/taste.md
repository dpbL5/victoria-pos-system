# workflow
- When asked to review/rewrite a todo list, verify each item against the actual codebase rather than reorganizing textually: survey the relevant source (via parallel explore sub-agents with an itemized status checklist, citing files/lines per item), then rewrite the list concisely grouped by status — done / not done / notes — keeping unfinished items and flagging genuinely missing features (e.g., "Kiếm tra lại todos của tôi xem đã thực hiện được ở dự án này chưa. Viết lại todo của tôi cho gọn lại."). Confidence: 0.65
- Keep the sub-agent/skill roster lean and project-specific: prune agents and skills that are rarely used for the current project rather than retaining a large generic set (e.g., after learning there were 60 sub-agents, user asked to remove ones rarely used for this project). Confidence: 0.65
- Keep documentation files (CLAUDE.md, AGENTS.md) in sync with structural code changes such as route consolidation, feature moves, or navigation updates. Confidence: 0.80
- For one-off data migrations (schema restructures), write a separate idempotent script wired as its own npm script (e.g., `migrate:walkin-customer-name`), executed after the DDL is applied, that runs inside transactions, copies values to the new field before detaching old references, and only deletes rows verified to have no remaining references (counting sessions/invoices/memberships first) — rather than raw inline SQL or throwaway code. Confidence: 0.70
- Prefers implementation work to be broken into explicit phases for easier rollout ("Chia ra các phase để dễ triển khai"): phases ordered by dependency and risk (quick cleanup first, then shared foundations, enforcement, adoption, legacy cleanup last), each independently shippable with its own verification step, with parallelizable phases called out explicitly. Confidence: 0.80
- Before implementing complex multi-file features or proposing a UI redesign, enter plan mode, explore existing code thoroughly, and write a structured implementation plan document covering all phases — including both business/payment logic calculation and UI design — before starting. The user also explicitly requests a design direction ("cho thiết kế cho tôi hướng") before unification/refactor work proceeds, rather than jumping straight to code. Confidence: 0.92
- Before planning any new feature, check whether it already exists in the codebase — and if it does, report exactly what is already implemented and what (if anything) still needs to be added. The user explicitly asked "Kiểm tra trước xem đã lưu chưa rồi lên kế hoạch có cần thêm hay không?" before requesting player name storage, and the response confirmed the feature already existed end-to-end (DB field, rename API, checkout flow, invoice display), so no new implementation was needed. When existing functionality is confirmed, report it clearly and stop — don't plan changes that aren't needed. Confidence: 0.85
- The user maintains spec documents in `docs/` (e.g., `docs/check-out.md` describing the target checkout layout) and drives implementation from them: when they point at one (`Sửa UI layout theo @docs/check-out.md`), read the document FIRST and treat it as the authoritative layout spec to follow faithfully — including checking backend/API capability for every interaction the spec implies (e.g., the doc drew per-line checkboxes on sell items → grep confirmed the `DELETE /api/sessions/[id]/sell-items` endpoint exists before building removal UX) — instead of asking for a verbal description or improvising the layout — and respect the doc's scope markers: where the doc says a part is "đã có hiện tại" (already exists / unchanged), leave that control exactly as it is rather than redesigning it (the agent converted the payment-method `Select` into a button grid, then reverted it back to the original `Select` because the doc didn't ask for that change). Confidence: 0.6
- When a user mentions multiple distinct features or changes, treat them as separate implementation tasks rather than bundling them into one plan. Confidence: 0.65
- Use `todo_write` to track phased, structured progress through complex features, updating individual task statuses as work progresses. Confidence: 0.75
- When a series of `edit_file` calls corrupts a file, read the full file and rewrite it entirely from scratch rather than attempting to fix broken incremental edits. Confidence: 0.80
- Before referencing a field on a DTO/return type that was previously unused on a given consumer (e.g., `shift._count.sessions` when the old code only used `_count.payments`), grep the API/route handler to confirm the field is actually selected/returned. The agent verified via `grep` that `select: { _count: { select: { sessions, payments } } }` was already in the shift route's Prisma query before referencing `shift._count?.sessions` in the UI; this is a tight feedback loop that catches "I assumed the API returns X" mistakes before they hit render-time undefined. Apply this any time a UI consumer starts depending on a new field — even if the field looks obviously relevant, the API contract is the source of truth and must be checked. Confidence: 0.75
- When a feature component receives data from a backend `ports.ts` interface but needs a new field that the port doesn't yet expose (e.g., a fresh `revenuePerPlayer` metric added to `TrendTotals`): duplicate the interface locally in the component file rather than (a) immediately refactoring the port's full type, or (b) importing the port and using `as` casts. The component defines a local `TrendData` interface mirroring the relevant subset, then a tiny tail edit on the port adds the same field, and both stay in sync via the wire contract — the local copy is the consumer's authoritative "this is the shape I'm parsing", the port is the producer's authoritative "this is the shape I'm returning", and the diff is one line in each file. This is faster than a cross-file refactor and avoids a temporary "cast through any" window. Document the local interface with a comment noting it mirrors `src/lib/<domain>/ports.ts`. Confidence: 0.7
- When establishing a UI pattern on one component, apply it consistently to all sibling/related components in the same change and proactively call out every consumer that needs updating — the user explicitly asked for "Tốt làm tương tự với @src/features/pos/active-session-card.tsx" and "Nhớ sửa cả playerPauseCard nhé" after a pattern was set on one file, expecting downstream propagation to be tracked as part of the same task rather than deferred to follow-up turns. This is the same propagation discipline used elsewhere (e.g., "Áp dụng chung cho cả hội viên", "Dọn dẹp tương tự với @...player-pause-card.tsx") — recognize it as a recurring pattern: when a fix lands on one component, immediately grep/list the sibling components that share the same shape and fix them in the same pass. Confidence: 0.85
- Organize tests by module/feature in `__tests__` directories, testing pure functions and Zod schemas with Vitest (`describe`/`it`/`expect` + `safeParse`). Confidence: 0.85
- Verify changes in this order: typecheck (`tsc --noEmit`) and ESLint in a single combined step, then run tests (`vitest run`), then production build (`next build`). Confidence: 0.76
- Commit changes as small, feature-grouped commits (prefixed with type like `feat`/`fix`/`chore`/`refactor`) rather than one large commit, each with a descriptive Vietnamese message and `Co-authored-by: CommandCodeBot` trailer — the user explicitly restated this preference and its rationale: "I prefer commits many changes for easy to control diff" (e.g., session work was split into separate UI / backend / test commits). Confidence: 0.97
- When removing a git worktree, fully clean up its lifecycle — verify the branch's changes are already merged (diff the branch against the target), `git worktree remove` (with `--force` if needed), manually `rmdir` any leftover empty directory that git couldn't remove, `git worktree prune` stale registrations, `git rm --cached` the stale gitlink from the index, commit that removal, and clear any leftover `.git/rebase-merge` state so `git status` no longer reports a phantom in-progress rebase. Confidence: 0.6
- The user expects the working tree to end fully committed, with nothing left behind: when asked whether to also commit leftover pre-session changes they replied "Có", and when the agent left debug/scratch files out (`resp.json`, `sessions.html`, `cookies.txt`, `.commandcode/taste/*`) the user followed up with a terse "commit th rest", and a one-line "remove worktree and commit all changes" covered cleanup of a worktree, stale gitlinks, and leftover rebase state — so ALL remaining changes (code and non-code alike, including debug artifacts, taste files, worktree removals, and stale `.git` state like `rebase-merge` directories) get handled so `git status` ends clean, reviewed via `git diff`, and grouped into topic-based `chore` commits; the agent may still flag credential-bearing files (e.g., `cookies.txt` containing a login session) for `.gitignore`, but should not refuse to commit them when asked. Confidence: 0.85
- When suggesting commit groups, merge any groups that touch the same file into a single commit rather than splitting them into separate commits that share a file. Confidence: 0.90
- After merging a feature/dev branch into `master` or `main`, delete the merged branch both locally (`git branch -d`) and remotely (`git push origin --delete`). Confidence: 0.90
- Parallelize independent exploration reads (e.g., reading schema.prisma + glob-ing barrel `index.ts` files + reading domain directories) in a single tool-use block to quickly map project structure before diving deeper. Confidence: 0.75
- When merging dev into master/main, use `--no-ff` to preserve branch history and create an explicit merge commit. Confidence: 0.80
- When a commit accidentally includes unrelated files (e.g., auto-generated config), use `git reset --soft HEAD~1` + `git restore --staged <unrelated>` to fix the commit scope without losing work. Confidence: 0.75
- When a local commit has already been pushed and is then amended locally (e.g., a stash/pop cycle during verification unstaged files, so the commit had to be `--amend`ed), the branch diverges from the remote and shows two same-message "duplicate node" commits in the graph — diagnose via `git reflog` + comparing `git log` local vs `origin/<branch>` + `git merge-base --is-ancestor` before doing anything; then present the force-push fix (`git push --force-with-lease origin <branch>` — not a bare `--force`) and get the user's confirmation before executing it, since it rewrites remote history; the user confirms promptly with a terse "có" when asked, after which the agent force-pushes and verifies local/remote are in sync (divergence `0 0`) before reporting. Confidence: 0.7
- Prefers finding and installing well-vetted community skills (skills.sh leaderboard, `npx skills` CLI) over building custom tooling from scratch; vet candidates by install counts, GitHub stars, and security audits, and present the trade-offs before installing. Confidence: 0.65
- Prefers extracting a shared derived display value into a single `useMemo` (computed once at component level, with deps) over duplicating an inline calculation in each render site — when the same derived value (e.g., paused seconds at checkout) was needed by both a modal description/title and the main content block, the agent refactored the duplicated IIFE logic into one `displayPausedSeconds = useMemo(...)` used by both surfaces, avoiding duplicate logic. Confidence: 0.55
- Run CLI installers/commands in non-interactive mode (e.g., `-y`) so they don't hang on interactive prompts during agent runs; if a command hangs, retry it with the non-interactive flag. Confidence: 0.70
- When a review yields multiple prioritized candidates, user prefers to work through the batch in one session (e.g., "Let do first 6 candidates") via a structured "grilling" decision-tree process: verify facts first, then ask round-based questions covering the whole decision frontier — each with concrete options and a clear recommendation — and wait for answers before unlocking dependent questions; the user tends to accept the recommended option wholesale rather than weighing each choice: in Round 1 they answered "Q1c the rest is a" (5 of 6 recommendations), and in Round 3 explicitly said "tôi chọn tất cả theo bạn khuyến nghị" (all 6) — effectively delegating the design decisions to the agent. Confidence: 0.80
- When facing architectural design decisions that affect user-visible behavior (e.g., pricing flow, delete scope, pause behavior), present structured `ask_user_question` prompts with concrete options each carrying a `(Recommended)` label and a description explaining the trade-off; the user often accepts the recommended option rather than weighing alternatives (e.g., chose the recommended "Tối ưu DB + giảm request" option over the bigger bootstrap-endpoint refactor and the connection-string change when optimizing /sessions). But this is NOT absolute: when the recommended option doesn't match how the business actually runs, the user overrides it — in the multi-pricing-group checkout fix they twice chose the non-recommended options ("Thu hết 1 lần" over "thu từng nhóm riêng", and "Chọn tay từng người" over "theo thứ tự"), so the agent should keep recommendations genuinely optional and be ready to implement the user's own choice. Confidence: 0.6
- When surveying code before designing a complex feature, dispatch multiple `explore` sub-agents in parallel (e.g., one for schema/relations, another for UI/API endpoints) to maximize exploration throughput rather than feeding them sequentially; this is a specific instance of the general parallelization principle applied to sub-agent exploration. Confidence: 0.55
- When refactoring a module, prefers to do the complete job in a single change — extraction plus deduplication of duplicated code paths plus test coverage together — over a minimal behavior-preserving extraction with cleanup and tests deferred to later steps (chose "extract + dedup + tests in the same PR" over the recommended minimal option). Within that single change, prefers a strict sequence of separate commits — extraction first, then deduplication, then tests last — rather than writing tests before the dedup step as a safety net (chose "trích → dọn → test, từng bước commit riêng" over the recommended test-first approach). Confidence: 0.65
- When ordering a batch of refactoring candidates, prefers risk-first sequencing: tackle the riskiest/highest-value candidates (e.g., core check-in/check-out transaction logic) before trivial cleanups (e.g., deleting migration shims) — deliberately overrode the agent's recommended trivial-first "quick wins" order (chose "b: rủi ro trước → trivial sau" over recommended "a: trivial trước → rủi ro sau", planning C1→C2→C3→C5→C6→C4). This applies even though the user otherwise delegates design decisions to the agent's recommendations. Confidence: 0.7
- When building a new shared/reusable component, prefers creating a dedicated demo/testing page or tab first to visually preview the component in isolation with real data before integrating it into production pages. Confidence: 0.70
- Prefers using specialized sub-agents (e.g., `UI_Finish-Gate_Reviewer`, QA agents) for quality-gate tasks — UX auditing, code review, and re-reviewing core business logic for logic holes — rather than performing audits manually; the user explicitly requests these agent reviews, including repeat passes over critical flows (e.g., "Đánh giá lại use-case check-in và check-out bằng agent QA một lần nữa xem có lỗ hổng hay lỗi logic nào không. Nhất là tính toán giờ chơi"), with special attention to money/time calculations (play-time math, pricing, preview-vs-checkout consistency). Confidence: 0.85
- Before finalizing a data migration/cleanup script, first survey the actual DB state with a temporary throwaway script (count/list candidate records and their relations) rather than relying on assumptions about data shapes (e.g., a `Khách #…` name pattern) — real data can differ from expectations and cleanup criteria must be adjusted to match reality; after running the migration, re-verify the resulting DB state (e.g., zero remaining candidates, no sessions left without a name) and delete the temporary scripts afterwards. Confidence: 0.70
- tsx runs in CJS mode in this project, so DB/script files must wrap their logic in an async `main()` with `.catch(...)`/`.finally(prisma.$disconnect)` instead of top-level await; prefer temporary script files (prefixed `_`) over inline `tsx -e` one-liners to avoid shell-escaping issues — and place them inside the project (e.g., `scripts/`), because the agent scratchpad lives outside the project and cannot resolve `@/` path aliases — then remove those temporary files after use. Confidence: 0.75
- When relocating a feature's logic to another stage of the flow (e.g., moving pricing selection from check-in to checkout), preserve existing capabilities (e.g., splitting a group across multiple pricing tables) rather than dropping them for simplicity — user chose to keep group splitting over the recommended single-price-table simplification. Confidence: 0.55
- When lint/typecheck surfaces warnings, first determine via `git diff` whether each warning is pre-existing or introduced by the current change; fix only the ones the change introduced and leave pre-existing warnings untouched to avoid scope creep (agent explicitly checked `git diff` on `today-shift-screen.tsx` to prove `Clock`/`Minus`/`Package` warnings predated the change, removed only its own leftover `CheckoutResponse` interface, and re-ran eslint excluding the pre-existing-noise file; in a later fix the agent ran the full lint, confirmed the 5 errors were all in other unrelated files, then ran `tsc --noEmit` and eslint scoped to just the edited file to prove the change itself was clean). Stronger variant seen during the bán kèm/retail refactor: the agent used `git stash` to measure the baseline type-error count and to prove a failing test (`PROMOTION_NOT_APPLICABLE`) already failed on the original branch — so pre-existing failures are explicitly verified against the baseline and excluded from the change's scope before reporting. Confidence: 0.72
- Design refinement uses a "strip" philosophy: diagnose the primary job of the interface (e.g., "collect payment"), then remove obstacles to that job — visual monotony, excessive nesting, overloaded CTAs, missing affordances — without gutting capability. Each fix is scoped to a single identified issue and tracked via todo_write. Confidence: 0.65
- When a design review surfaces multiple distinct issue classes (motion, layout, hierarchy, color, copy), the user picks one design mode per turn and works through them sequentially as separate passes — the agent presented a structured `/interaction` (motion) recommendation first, the user picked "toàn bộ motion pass" (full motion pass) covering 7 files in one batch with a todo_write breakdown, and only after that finished did the next turn land a `/relayout` pass (ShiftRail composition rework). The motion pass itself was: (1) audit the current motion system (`globals.css` keyframes + `prefers-reduced-motion` kill-switch + per-component usage), (2) list concrete issues with file:line, (3) group by primitive vs per-screen, (4) present a 2/3-option scope question (full / narrow / minimal) with `(Recommended)` labeled, and (5) execute the chosen scope via todo_write. Apply the same structure to subsequent design modes (relayout, refine, recolor) — audit → list → group → scope question → todo_write → execute → verify. Confidence: 0.75
- After a structural layout change to a real component, the matching skeleton must be updated in the SAME pass to mirror the new structure — otherwise the page jump on data load will re-introduce the visual instability the new layout was meant to prevent. The agent updated `TodayShiftSkeleton` to mirror the new `ShiftRail` composition (header shape, 3-col stats with the accent stat taller, action bar 2-col mobile / 4-btn desktop, sessions section header + 2 list rows) right after rewriting `ShiftRail`, before declaring the layout work done. Skeleton fidelity is part of the layout deliverable, not a separate cleanup. Confidence: 0.8
- After picking one option from a multi-choice architectural decision, the user may follow up by asking about the alternatives they didn't pick (e.g., chosen "bỏ Payment" then asked "nếu bỏ MembershipPayment thì sao?") — they want a full pro/con breakdown of the other direction before committing; the agent should treat the initial choice as provisional and be ready to analyze tradeoffs of the unchosen paths without treating it as indecisiveness. Confidence: 0.6
- After a QA/review report classifies findings by severity (critical/major/minor), the user follows up with a terse directive to fix the top tier immediately in one batch (e.g., "Sửa luôn các lỗi Major" after the QA report listed 4 Major + 8 Minor) — implement ALL findings of that tier without per-item confirmation, track each via todo_write, and verify with typecheck + the full test suite + lint before summarizing; lower tiers stay deferred unless explicitly requested (here the user picked only the Majors, not the agent's broader recommendation to also fix cheap minors). Confidence: 0.6
- When the user says "continue" after a design review + refinement pass, proceed autonomously to the next recommended design mode (as listed in the review report's recommendations) without pausing for confirmation — the user expects the agent to self-direct through the full review-to-fix pipeline. Confidence: 0.7
- A bare "continue" is the user's whole approval signal: it doubles as plan acceptance after `exit_plan_mode`, meaning execute the ENTIRE saved plan document end-to-end in one autonomous pass (schema → ports → adapter → use-case → API route → UI → tests → `prisma db push`/`generate` → tsc/vitest/eslint verification) without re-asking per step, re-presenting the plan, or stopping mid-way for check-ins; the user may repeat "continue" purely to unblock a long run, not to change scope. Confidence: 0.75
- Design reviews follow a structured report format: header with target/score/date, TL;DR verdict, 5 named heuristic scores (First impression, Hierarchy, Color voice, Type voice, Interaction feel — each out of 10), cognitive load assessment rated High/Medium/Low with PASS/WATCH/FAIL items, "What's Working" section, priority issues (P0–P6 by functional severity: P0=functional blocker, P1=cognitive overload, P2+=hierarchy/clutter/brand), smell detection (template tells), and ordered recommendations each mapped to a design mode (/interaction, /relayout, /recolor, /refine). Confidence: 0.7
- Design review reports are saved as both `.md` and `.html` in `.commandcode/design/`. The HTML version uses a dark theme with monospace `// SECTION` headers, corner-box accent decorations, and a color-coded priority issue list (rose for P0-P2, amber for functional, purple for brand). Confidence: 0.65
- When a major refactoring decision requires a DB migration (schema change + data backfill), the user expects a structured sequence: (1) backup/old-data dump first, (2) Prisma schema edit + `db push --accept-data-loss`, (3) backfill from backup via script, (4) code changes, (5) verify tsc + vitest. The backfill script should use `@/generated/prisma` with the PrismaPg adapter pattern (not raw psql, not raw pg), read old data from a JSON backup file (since the Prisma client no longer has the dropped model), and be idempotent. Confidence: 0.7
- The user expects full pro/con comparison tables (with a summary row showing: code changes needed, backfill complexity, migration risk, long-term cleanliness) when presented with two architectural options, even when they've already tentatively chosen one — they may reverse their choice after seeing the full tradeoff analysis (initially chose "bỏ Payment" then reversed to STI after seeing the comparison). Confidence: 0.7
- When the user tersely asks to "commit changes" or "commit changes and push" (a single directive covering the whole git workflow), they expect the agent to run the full sequence autonomously without per-step confirmation: inspect status/diff (including untracked dirs and a peek for anything sensitive) to characterize the change, run the test suite first (plus typecheck) to avoid committing a broken state, stage everything, write one descriptive commit message summarizing the batch's contents, and push only if explicitly asked — otherwise report the resulting commit hash and push readiness (e.g., "Committed `648000b` ... pushed to `origin/dev`"; or "Committed `5a737b3` ... ready to push whenever you want" when push wasn't requested). Confidence: 0.78
- When an interactive rebase is in progress and paused mid-way (e.g., stopped at a commit to amend), and the user asks to commit changes, the natural path is to fold the working-tree changes into the paused commit (`git commit --amend`) and then finish with `git rebase --continue` — not to abort the rebase, start a new separate commit, or re-run the whole rebase; verify the full git state first (branch, rebase todo, stash) and report the final state after continuing (e.g., `dev` clean on top of the new base). Confidence: 0.8
- This project's development work happens on the `dev` branch (commits are pushed to `origin/dev`), with `master`/`main` as the release target that feature/dev branches get merged into. Confidence: 0.6
- When the Next.js dev server fails with Turbopack cache-corruption errors (`TurbopackInternalError: Failed to restore task data (corrupted database or bug)`, missing `.sst` files under `.next/dev/cache/turbopack`, `ENOENT` on `.next/dev/server/.../build-manifest.json`), the fix is to stop the dev server, delete the corrupted `.next/dev/cache/turbopack` directory, restart, and smoke-test a page via curl (e.g., a 307 redirect to `/login` without an auth cookie counts as a clean compile); also check the restarted server log for residual errors. Confidence: 0.7
- When the agent is in plan mode and the user asks for an implementation (not just an analysis), recognize the constraint and present a concrete fix — explain the root cause with file:line references, show the exact code change(s) the agent WOULD make, and explicitly ask the user to exit plan mode ("nhấn Shift+Tab để thoát plan mode rồi cho tôi biết nhé") rather than attempting `edit_file` and failing; the user expects the investigation + proposed fix to be fully ready to apply the moment plan mode is off, so they can review and approve in one pass. Confidence: 0.7
- Before killing processes during dev-server troubleshooting, verify ownership first (via `/proc/<pid>/cwd` and cmdline) so that a stale dev server belonging to a different project/app (e.g., a different app's `next-server`) is left untouched — only kill processes confirmed to belong to the current project. Confidence: 0.65
- When the user requests flow documentation/diagrams (e.g., "Vẽ tôi sequence diagram cho luồng bán kèm và check out -> markdown"), don't draw from assumptions: first dispatch a depth-thorough `explore` sub-agent to trace the real implementation — exact file paths, function names, actor layers (UI → route handler → use-case → repositories/Prisma), the step-by-step ordering inside transactions, merge/consume mechanics of related records, and any Vietnamese error/note strings — then render the sequence diagram(s) in markdown (Mermaid) using those verified details. The user specifies the deliverable format tersely inline (e.g., "-> markdown"). Confidence: 0.6
- After deleting a page/route, if typecheck or build surfaces errors referencing the deleted route (stale `.next` build-cache references, not actual code in source), clear the `.next` build cache directory (`rm -rf .next`) and re-run rather than investigating phantom code references — this is distinct from the Turbopack dev-server cache-corruption scenario. Confidence: 0.65
- When debugging a data/state anomaly (e.g., "after rename + reload, players reshuffle"), trace systematically through the full stack: UI render → screen state mutation → API call → use-case (write path) → adapter/repository (Prisma) → DB row — reading each layer in parallel where independent, citing exact file:line, and comparing sibling code paths (e.g., `findMany` vs `findByIdWithPlayers`) for inconsistencies. The investigation should explicitly distinguish "what the code is doing" from "what the user observed" and explain the chain of events (e.g., "createMany → same `createdAt` → UPDATE changes CTID → `ORDER BY createdAt` becomes non-deterministic") before proposing fixes, then present multiple fix options (no-migration / schema-change / partial) with the agent's recommendation and wait for the user to choose before implementing — schema changes are not made without explicit confirmation. Confidence: 0.7
- When the user asks to temporarily disable a UI feature/button ("Tạm thời disable nút Bán lẻ"), implement it reversibly rather than deleting code: gate the trigger behind a new optional prop that defaults to the enabled state (so other consumers are unaffected — grep all usages to confirm), keep the underlying dialog/state/imports intact so re-enabling is trivial, and don't treat the now-unreachable code as dead code to clean up (the usual "delete now-unused imports/state" rule is deliberately suspended for explicitly-temporary disables). Confidence: 0.7
 When committing after a "commit changes" directive, treat tool-internal non-code directories as non-committable: untracked tool dirs (e.g., `.commandcode/plans/`, `.impeccable/`) are added to `.gitignore` rather than committed, while already-tracked tool config (`.commandcode/` containing agents/skills/taste) is treated as legitimate project config and committed along with the rest of the working tree — the agent checked tracked status (`git ls-files`) before deciding, and committed all remaining changes (including the tracked taste files) in one comprehensive commit. Confidence: 0.5
- When a lint rule rejects the chosen implementation, rewrite the approach to satisfy the rule instead of silencing it: `Date.now()` in render → a tick `useState` driven by `setInterval`; a module-level ref/id stack for "which modal is on top" → DOM-order detection via a data attribute. Narrow, paired `/* eslint-disable-next-line */`-style block comments (with a matching `enable`) are the accepted escape hatch only for the unavoidable framework pattern of `setState` inside `useEffect` (used to reset drawer state when a new session opens) and for `@next/next/no-img-element` on native `<img>`. After each restructure, re-run eslint + scoped `tsc --noEmit` (filtering out pre-existing `__tests__` noise with `grep -v`) and then grep for the component's consumers and run the full `vitest` suite before reporting done. Confidence: 0.7
- When a move-this-feature instruction is ambiguous about the destination ("chuyển nút vào trong trang Cài đặt" — but two distinct "settings" pages exist: `/customers/[id]` customer detail vs `/settings` global settings), do NOT pick a destination by assumption: present a structured `ask_user_question` with each candidate labeled by its actual route + purpose, and ask the second related question in the same call (e.g., "do we keep the button on the drawer too, or remove it from every former location?"). Bundle 2-3 tightly-related scope questions in one `ask_user_question` call so the user answers the whole decision frontier in one pass before implementation starts. The user answers tersely and the agent then has enough scope clarity to design the extraction. Confidence: 0.75
- When `edit_file` with a large `old_string` block fails to match, do NOT keep retrying the same big block — split it into smaller targeted edits (e.g., one function at a time: `handlePausePlayer` separate from `handleResumePlayer` rather than both in one 60-line `old_string`). If invisible characters are suspected, inspect with `cat -A | head -50` or `od -c` to look for non-breaking spaces, tab vs space mismatches, or trailing-newline differences before re-attempting; once the file is verified clean (e.g., all LF, consistent indentation), the smaller blocks match reliably. Confidence: 0.78
- The `// eslint-disable-next-line <rule>` directive disables the NEXT **statement**, not the next line — for multi-line expressions, the comment must sit on the line containing the impure call, not on the line above the expression that opens it. After a `react-hooks/purity` complaint, read the source line number in the lint message and place the comment on that exact line; otherwise the rule still triggers because the offending call is in a later line of the same statement. The same applies to any other directive that operates on the "next line" — verify the position by the source location the linter actually reports. Confidence: 0.75
- Hooks must always be called above every early return in a component — even `useState(() => ...)` initializers and `useMemo` that look "safe" (one-time, no side effects) trigger the Rules of Hooks violation if placed after `if (loading) return <Skeleton/>` because the early return may run on a different render than the rest. When refactoring introduces a new hook that "belongs" near a downstream computation, the hook goes UP next to the other hooks, then the early returns; compute downstream consumers from the (possibly null) value the hook returned. Don't try to "reserve space" with a `useState` placed after the early return. Confidence: 0.85
- When `npm install <runtime-package>` (e.g., `npm install chart.js`) strips devDeps from `node_modules` — this project has been observed to lose `eslint`, `next`, and other devDeps on a runtime-deps install — run `npm install --include=dev` immediately after to restore them. The signature symptom is a missing `node_modules/.bin/next` or `node_modules/eslint` directory; verify with `ls node_modules/next/package.json` after every runtime-deps install and before running `next build` or `eslint`. If `npx next` is missing, npx will silently pull a different (latest) `next` version with stricter parsers and produce confusing `CssSyntaxError` / `@theme inline ... Invalid custom property` errors that aren't actually in the source. Always restore devDeps from the project's lockfile before debugging. Confidence: 0.85
- When a build error references a line deep in `node_modules/tailwindcss/index.css` or `node_modules/<other-dep>/...` and no source file was recently changed to justify it, do NOT trust the error as a real CSS or postcss bug — first check whether `node_modules/next` (and other core devDeps) still exist. The CSS error is a downstream effect of `npx` silently pulling a different `next` version, which uses a stricter CSS parser. Fix: `ls node_modules/next/package.json` → if missing, `npm install --include=dev` to restore from the project's lockfile → re-run the build. Don't `rm -rf node_modules` and re-install everything unless the lockfile itself is suspect; a targeted `npm install --include=dev` is faster and reliable. This is distinct from the Turbopack dev-cache corruption scenario (`.next/dev/cache/turbopack`) — that one shows `TurbopackInternalError: Failed to restore task data`, not a CSS parse error. Confidence: 0.8
- When the user says "bỏ qua, reapply lại thay đổi" (or equivalent) after a destructive undo / rollback, re-derive the implementation from the conversation context and apply it cleanly — do NOT try to restore from `git stash`, partial reverts, or scattered file diffs; treat the conversation as the source of truth for what needs to be re-built. Verify each new install/edit with a clean build (e.g., `./node_modules/.bin/next build` using the local binary, not `npx next build`) to confirm the reapplication is self-consistent before reporting done. The user expects the original intent to land, not a half-restored state. Confidence: 0.7
- When the same chain of operations that produced a bug needs to be re-run in isolation to confirm a fix, check the LAYER first before running tests — for example, when `npx next` is missing, the local `./node_modules/.bin/next` exists and works; always prefer the local binary to avoid npx pulling a different version. The diagnostic chain is: verify the binary exists → run with the explicit path → if it still fails, the issue is in the source; if it succeeds, the previous failure was a tool-chain artifact. Apply this discipline to other tools too (eslint, tsc, vitest) — prefer `node_modules/.bin/<tool>` over `npx <tool>` when `node_modules` is in a transient state. Confidence: 0.7
- When using PowerShell to read files with literal bracket characters in the path (e.g., Next.js dynamic route files like `sessions/[id]/route.ts`), use `-LiteralPath` instead of `-Path` or bare positional args — `Get-Content -LiteralPath "file with [id]" -Encoding UTF8` handles brackets correctly, while `-Path` interprets them as wildcard characters and throws errors. This is a recurring Windows path issue in this project; when a bare path command fails, retry with `-LiteralPath` before switching to `glob` or other fallbacks. Confidence: 0.75
- A Next.js 16 / Turbopack build error that reports a *syntax* error on code that reads as valid (e.g. `Expected '{', got 'ident'` pointing at the identifier of an ordinary `export type X = Prisma.SessionGetPayload<{...}>` in `src/lib/<domain>/ports.ts`) is frequently a phantom: a parse-cache snapshot taken mid-save by the live `next dev` watcher, not a real defect. Before touching the source, prove whether the code is actually broken — batch-parse every file in the error's `Import trace` with the TypeScript compiler API (`node -e` + `ts.createSourceFile(...)` and read `sf.parseDiagnostics`, using `ts.ScriptKind.TSX` for `.tsx`), plus `od -c` on the flagged line to rule out invisible characters/BOM/encoding. If the files parse clean, do NOT "fix" valid code: kill the dev-server PIDs, `rm -rf .next/dev` (the Turbopack dev cache, distinct from `rm -rf .next` for build-cache and from `.next/dev/cache/turbopack` for `TurbopackInternalError` corruption), restart, and smoke-test with `curl -o /dev/null -w "%{http_code}"` against both the page route and the failing API route to confirm 200. Also check whether a schema file was edited in the same window (verify the field exists in `prisma/schema.prisma` AND in `src/generated/prisma/`) so a stale generated client doesn't become the next failure. Tell the user the final manual step (`npm run dev` + refresh, since an open tab replays the cached overlay) and the one-line remedy to reuse if it recurs. Confidence: 0.7
- When the user disputes a claimed fix ("Tooi xác nhận lỗi chưa được sửa."), do NOT restate the previous conclusion — escalate to strictly stronger, independent evidence and re-run the investigation from scratch. The escalation ladder used here: (1) prove the on-disk file is unchanged/clean (`md5sum` + `sed -n` of the flagged lines + `git status --short` on the dir), (2) re-parse the file with the TypeScript compiler API, (3) wipe the whole `.next` dir (not just `.next/dev`) and restart, (4) check whether a *second* dev server is occupying an adjacent port and whether anything runs in docker (`ss -tlnp | grep -E ":300[0-9]"`, `ps -eo pid,ppid,lstart,tty,etime,cmd`, `docker ps`) so the user isn't looking at a different process, (5) run `next build` — the decisive test, since it uses the same Turbopack parser with zero dev cache, (6) hit the route end-to-end with real authentication, and (7) only then conclude the symptom is client-side (a frozen Next error overlay in an already-open tab) and hand the user one concrete manual step plus a precise fallback ask if it persists ("đóng hẳn tab, mở tab mới / Ctrl+Shift+R; nếu vẫn lỗi gửi tôi screenshot kèm URL chính xác"). Confidence: 0.8
- To verify an auth-protected Next.js route end-to-end without bothering the user for credentials, find the seeded account in the repo and drive it with curl: grep `prisma/seed*.ts` (e.g., `prisma/seed-admin.ts`) for the username/password, read `src/app/api/auth/login/route.ts` to learn the expected body and CSRF mechanism, then `curl -c <cookiejar> -X POST /api/auth/login -d '{"username":"admin","password":"…"}'`, extract the csrf cookie into a header, and call the target API with `-b <cookiejar> -H "x-csrf-token: $CSRF"` plus the page route with `-L -o /dev/null -w "HTTP %{http_code}, %{size_download} bytes"` — a 200 with real payload data proves the whole import chain compiled. Store the cookie jar in the scratchpad, not the repo. Confidence: 0.7
