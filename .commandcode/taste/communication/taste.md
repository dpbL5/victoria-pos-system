# communication
- Communicate in Vietnamese for this project. Confidence: 0.85
- When user asks "Lỗi này từ đâu vậy?" / "tìm hiểu nguyên nhân và sửa lỗi" about an error while only pasting the raw console/dev-server log (stack traces like `TypeError: ... is not a function` plus request timing lines like `POST /api/products/... 500 in 1226ms`), they expect the agent to trace the error to its actual source AND fix it — systematically eliminate candidates (grep for the failing call across the codebase, read the failing helper's signature vs. its call sites, inspect all candidate routes/components) and reason from evidence rather than guessing; asking one targeted clarifying question (e.g., when the error appears) is acceptable when the root cause can't be pinned down from logs alone. Confidence: 0.70
- User reports bugs as concrete step-by-step reproduction flows (exact navigation path, button labels, observed error message) plus the business risk of repeating the action (e.g., "ấn nhiều lần tạo nhiều customer") — expects the agent to reason through the full sequence and side-effect risks, not just the headline error. Confidence: 0.55
- User also reports bugs as a single terse line naming the failing condition in business terms (e.g., "Sửa lỗi số người checkout lớn hơn số người còn lại nhóm giá") with no logs or repro steps — they expect the agent to trace the root cause from the code itself (read the relevant use-case, reason about stale vs. current state in transactions), fix it, and verify with typecheck + tests, without asking clarifying questions. Confidence: 0.5
- Khi người dùng nói "bỏ nó đi" hoặc tỏ vẻ muốn xoá bỏ một tính năng trong lúc bực bội, hãy xác nhận lại phạm vi chính xác trước khi xoá code — tránh hiểu nhầm thành xoá toàn bộ thay vì đơn giản hoá/làm gọn. Confidence: 0.65
- User occasionally types terse commands in English ("continue", "Let do first 6 candidates", "all i choose a", "commit changes", "I need you to remove agents/skills that I would rarely use for this projects", "commit th rest") even in this Vietnamese-speaking project; respond in Vietnamese regardless. Confidence: 0.80
- When commissioning a QA/review agent to audit business logic, the user's prompt template specifies Vietnamese output, a list format with NO code edits, an explicit severity classification (critical/major/minor), and per-finding details of file:line, error description, triggering scenario, and a brief fix suggestion — the agent is expected to mirror this structured template, and to deliver a synthesized prioritized finding list (e.g., "4 Major, 8 Minor, không có Critical") that distinguishes must-fix money/time-affecting issues (play-hours math, preview-vs-checkout mismatch, membership expiry, double-billing) from cheap minor guards. Confidence: 0.6
