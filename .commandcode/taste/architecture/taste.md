# architecture
- Extract use-case functions from route handlers into src/lib/business/use-cases/ for check-in, checkout, member registration/renewal, and shift close. Confidence: 0.70
- When migrating to a new architectural pattern, fully eliminate the old pattern across the entire codebase rather than maintaining a hybrid state where old and new patterns coexist (e.g., fully migrate route handlers away from direct DB access instead of leaving some routes on the old pattern). The user explicitly chose complete unification over partial migration when refactoring route handlers to the use-case pattern. Confidence: 0.85
- Mark client-side components with the `'use client'` directive at the top of the file (Next.js App Router convention). Confidence: 0.80
- Add DB-level constraints (partial unique index for single open shift, check constraints for invoice totals, no overlapping memberships) to enforce business invariants. Confidence: 0.70
- Use Asia/Ho_Chi_Minh timezone explicitly in report grouping and pricing date calculations instead of server local time — and generate day-group keys with ONE canonical timezone-aware formatter (e.g., `toInputDate`) across all grouped surfaces: mixing `toDateString()` (server/US local) with `toInputDate()` (VN) makes the same VN calendar day produce two different keys and triggers React duplicate-key warnings ("Encountered two children with the same key, `2026-08-13`"). This bug class recurs across multiple surfaces — day-grouped lists (shifts screen, activity log) AND chart data points (AreaChart `<circle key={d.label}>` where the label is a date string) — so when it appears, audit every surface keyed by a day/date string, including chart series, not just list keys. Confidence: 0.82
- Move generic helpers (api.ts, format.ts, types.ts) from features/pos/ into src/lib/. Confidence: 0.60
- Hide /pricing tab from staff users in bottom navigation; keep exactly 5 staff tabs. Confidence: 0.65
- Align cost rounding with documented rule: ceiling to nearest 10,000 VND instead of Math.round. Confidence: 0.65
- Organize features by domain module (e.g., shifts module) rather than by report type; when a report-type feature fits better in a domain module, move it there and remove it from the report section. Confidence: 0.85
- When explaining complex business logic flows (e.g., check-in → payment → checkout lifecycle), present the full state machine as an ASCII diagram with explicit states and named transitions, plus a summary table of steps/endpoints — rather than prose-only descriptions. Confidence: 0.65
- Present a dataflow diagram alongside the state machine when explaining a business flow's architecture — showing how data/entities move through the layers (UI → API route → use-case → repository/adapter → DB), which entities are created/updated at each step, and how flows join/split (e.g., where bán kèm items merge into the checkout invoice) — the user explicitly asks for "state machine và dataflow" together, indicating they want the lifecycle view alongside the data-movement view, not prose-only descriptions. Confidence: 0.70
- Prefer unified/consistent business logic over special-cased hardcoded branches: when structurally similar features (e.g., member checkout vs. walk-in checkout) diverge in implementation, unify them onto a single shared logic path rather than maintaining separate branches with hardcoded values. The same applies when the same concept is modeled two ways in parallel — session-level pause (`Session.pausedAt`/`totalPausedSeconds`, used for single-player sessions) vs player-level pause (`SessionPlayer`, used for group sessions) — the user explicitly asks for a design to synchronize them onto one source of truth ("cho thiết kế cho tôi hướng để có thể đồng bộ được 2 logic này với nhau"), with the working variant (player-based) becoming the target model for all variants. Confidence: 0.8
- Avoid hardcoding business-domain values (e.g., `payment method = MEMBER`); instead use the established dynamic/logic-based assignment that other parallel code paths already rely on (e.g., walk-in payment logic). Confidence: 0.75
- When documenting or summarizing data models, organize them into business domain groups (e.g., Identity & Auth, Customer, Pricing, Memberships) and cross-reference each model with its corresponding `src/lib/` domain folder; note architectural details like barrel file existence, shared enums, and cross-domain usage. Confidence: 0.80
- Include enum value variants inline when describing model fields (e.g., `role` (`ADMIN|STAFF`), `type` (`WALK_IN|MEMBER`)) to provide complete context in domain summaries. Confidence: 0.70
- Enforce a hard three-layer boundary between Shared contract (isomorphic DTOs/Zod/constants — no Prisma), Server (prisma, ports/adapters, use-cases, auth/CSRF — marked `import 'server-only'`), and Client (API client helpers, features, components, hooks): use `server-only`/`client-only` markers plus ESLint `no-restricted-imports` so boundaries hold by enforcement rather than discipline; client code must never import domain barrels that transitively pull in Prisma. Confidence: 0.85
- The global `*:focus-visible` ring color must be independently legible in both light and dark themes — never use `--color-brand` if it resolves to near-black in dark mode. The fix: define a dedicated `--color-focus-ring` variable (e.g., `#2563eb` light / `#3b82f6` dark) in `globals.css` and reference it from the focus rule. Confidence: 0.7
- Avoid the "create-then-cancel" lifecycle anti-pattern: do not create an intermediate persistent record (e.g., a DRAFT invoice) in one step of a workflow solely to cancel/delete it in a later step — the user explicitly flagged this as wasteful and redundant ("rất tốn và không cần thiết làm dữ thừa dữ liệu"), directing that data attach directly to the final settled state instead. Generalizes to any throwaway intermediate record whose only purpose is to be undone in the same workflow: eliminate the intermediary and fold the data straight into the destination record. Concrete instance: the user asked to stop creating the bán kèm SEL/DRAFT invoice entirely ("Tôi muốn bán kèm vào phiên thì không tạo hóa đơn prefix SEL nữa tại vì nó áp dụng vào trong hóa đơn INV chính") — pending sell items are staged on the session and folded directly into the single INV invoice at checkout, instead of a DRAFT invoice that gets merged and cancelled later. Confidence: 0.95
- Route handlers must map repository/prisma shapes (e.g., `Prisma.SessionGetPayload` rows) to plain serializable DTOs (date as `string`, money as `number`) before returning, so the wire contract is explicit and decoupled from Prisma types. Confidence: 0.80
- When introducing new repository ports (e.g., a read-side ReportingRepository), prefers a single comprehensive port mirroring existing codebase patterns (like the existing BillingRepository) over granular per-feature method interfaces — even when an architecture review recommends the more "deep module" granular shape. Confidence: 0.55
- For read-side features without complex business rules (e.g., reporting queries), route handlers should call the repository port directly (validate + authorize + `repositories.reporting.*`) rather than adding an extra use-case layer — a use-case layer for pure read-side logic is over-engineering (chose route-direct over a `getDashboardReport` use-case). Confidence: 0.65
- When two domains duplicate similar pure logic (e.g., pricing/promotions day-overlap helpers), prefers extracting a shared generic module parameterized by field names/config over keeping per-domain copies or a minimal merge — chose the generic shared-module option over the recommended minimal merge that keeps domain-specific parts (like Prisma ruleWhere) local. Confidence: 0.60
- Prefer a lean data model: avoid creating persistent entity records for transient/anonymous data when a simple field on the existing record suffices. The user explicitly flagged auto-creating an anonymous `Customer` row per walk-in check-in as wasteful ("phí tài nguyên") and confirmed the lightest design — store the guest name directly on `Session` (`customerName`) with `customerId` nullable, only creating a `Customer` when a membership is actually registered. Confidence: 0.9
- When staging transient data across workflow steps (e.g., bán kèm items between sell and checkout), prefer reusing an existing model with an inline/array field (e.g., a JSON column on `Session`) over creating a new staging model/table — the user explicitly pushed back on creating a new `PendingSellItem` model during the bán kèm refactor ("không muốn tạo thêm model nào mới"), signaling that even transient staging data should avoid a new schema entity when an existing model can hold it. Confidence: 0.85
- Schema restructures must be non-destructive and migration-safe ("không làm hỏng db"): preserve existing data by copying values to the new field, only removing rows provably unreferenced (e.g., anonymous walk-in customers with no membership), and keeping records that back other features intact — never drop or break data that other flows still depend on. Confidence: 0.75
- Retried multi-step flows must not duplicate side effects (explicitly flagged risk: "ấn Gia hạn & check-in nhiều lần tạo ra nhiều customer"): after an earlier step succeeds but a later one fails (e.g., register membership OK, check-in fails), update client state so a retry performs only the failed step instead of re-running the completed mutation and creating duplicate customers/memberships/invoices. Confidence: 0.60
- Avoid duplicating the same business flow across multiple UI surfaces: when a flow (e.g., membership register/renew) already has a dedicated screen, keep that screen as the single entry point and have other surfaces (e.g., the check-in dialog) delegate via navigation rather than re-implementing the logic — duplicated entry points drift and multiply side-effect risk (user resolved the duplicate-customer bug by removing register/renew from the dialog entirely and routing to the membership tab). Confidence: 0.75
- Soft-delete user-facing entities that carry financial/audit history (e.g., members) rather than hard-deleting: add a `deletedAt` column, default-filter it out of `findById`/`findMany` so deleted entities vanish from lists, search, and check-in flows, and expose dedicated `softDelete(id, at)` + `findByIdIncludingDeleted(id)` repository methods so the delete use-case can distinguish "not found" vs "already deleted". Block deletion while the entity has live dependents (e.g., an ACTIVE session) with a 409 error telling the user to resolve it first; never touch financial records (invoices/payments) in the delete path. Confidence: 0.75
- Use-cases that wrap DB writes follow a two-phase structure: pre-transaction guards (validate + authorize, returning typed errors) then an exported `runXxxTx(tx, input)` transaction body; tests call `runXxxTx` directly with fake repositories instead of mocking Prisma's `$transaction`. Confidence: 0.65
- When adding a new transient state to an entity (e.g., pausing a session), prefer a nullable timestamp field (`pausedAt`) and derive the state from it rather than expanding the status enum — this preserves backward compatibility with every existing filter/query on the original status values across the codebase, and avoids cascading changes to reports, list endpoints, and business logic that all filter on `status='ACTIVE'`. Confidence: 0.65
- Don't rely on `createdAt` alone for stable ordering of rows that may share a timestamp (e.g., `SessionPlayer` rows created in the same `createMany` transaction get identical `createdAt`, then UPDATE on one row changes its CTID/physical position making `ORDER BY createdAt ASC` non-deterministic on subsequent reads). Fix by adding an explicit `position` (Int) column populated at creation time (`0, 1, 2, ...`) and ordering by `position ASC` — this reflects real business meaning (check-in order) and survives UPDATEs. When auditing existing code, compare `orderBy` between sibling adapter methods (e.g., `findMany` vs `findByIdWithPlayers`): a missing `orderBy` on one path is a latent bug even if the other path is currently correct. Confidence: 0.75
- Present a code-only "no-schema-change" option first when a determinism/ordering bug has one, but treat the user's no-schema-change preference as provisional, not absolute: the user initially rejected the recommended `position` (Int) column migration for stable `SessionPlayer` ordering ("Hãy tìm cách giải quyết khác không phải thay đổi schema") and the code-level fix (forcing distinct `createdAt` via `new Date(Date.now() + i)`, plus adding the missing `orderBy` on `findByIdWithPlayers`) was implemented, but on the very next turn the user reversed and accepted the schema change ("Thôi được rồi thêm id riêng đi vậy") — they wanted the proper fix (a `position` Int column with `@@index([groupId, position])`, populated at create time, ordered by `position ASC`, the column being a stable sort key that doesn't depend on `createdAt`/`CTID` and doesn't shift on UPDATE). So lead with the no-schema-change option, but be ready to implement the schema change the moment the user says so, without re-litigating the trade-off. Confidence: 0.7
- When a display/anomaly bug could be fixed either at the UI fallback layer (`name?.trim() || \`Người ${index + 1}\`` re-derives the value on every render) or at the data-creation layer (assign the same value as a real `name` field when the row is created), prefer the data-creation fix: the value is persisted in DB and read back as-is, so reloads are stable, future renames survive, and the UI's fallback becomes a defensive net-of-safety rather than the primary path. The user's framing: "Thay vì gán lại tên mỗi lần reload, ta gán tên cố định từ khi mới tạo phiên thì sao?" — they consistently push back on runtime re-derivation of values that should be stored at write time. Confidence: 0.85
- Route handlers must never cast the `repositories` bundle (`repositories as never`) to satisfy helpers that expect a Prisma store/delegate — the cast hides the mismatch until runtime (e.g., `db.shift.findFirst is not a function` → 500). Use the domain port exposed on the repository (e.g., `repositories.shift.findOpenForStaff(staffId)`) for domain queries, and pass `prisma` from infrastructure explicitly to read helpers that genuinely need Prisma delegates (e.g., `getShiftTransactions(prisma, id)`), consistent with the three-layer boundary. Confidence: 0.6
- When a data model flaw creates duplicate records for the same business event (e.g., Payment + MembershipPayment both recording the same membership fee), fix at the write-side (stop creating the duplicate) rather than compensating at the read-side (merge/filter in helpers) — even when the source fix is riskier and touches more files. The user explicitly chose the non-recommended "Bỏ tạo Payment khi đăng ký" option (remove Payment creation in use-case) over the agent-recommended "Merge ở tầng đọc" (patch in read helpers), prioritizing clean data architecture over short-term safety. Confidence: 0.8
- Prefer Single-Table Inheritance (STI) over multi-table split when two entities represent the same business concept (e.g., Payment + MembershipPayment both recording money collected). When offered a choice between keeping two separate tables (safer) and merging via a discriminator column (riskier migration, cleaner result), the user chose STI — driven by "1 bảng, 1 nguồn sự thật" (one table, one source of truth). This applies when: (a) the two tables share most fields (invoiceId, shiftId, staffId, amount, paymentMethod, paidAt), (b) one is a strict subset of the other, and (c) every read-side consumer currently has to merge/union both tables. Confidence: 0.85
- In long transactions that both move related rows and decrement counters (e.g., multi-pricing-group checkout: `movePlayersToGroup` + `decrementGroupRemaining`), never derive decrement counts from a stale in-memory context snapshot (e.g., `session.pricingGroups` captured before the moves) — instead decrement per group from the actual selection counts (`playersToBillByGroup[i]`) against the real persisted group ids (write back the created id into the pending assignment object so the decrement loop can use it); this class of bug surfaces as `PRICING_GROUP_UNDERFLOW` ("số người checkout lớn hơn số người còn lại nhóm giá") when the same players are counted against their old group. Confidence: 0.6
- When a shared sticky header needs to host a page-scoped action (e.g., a refresh button on mobile), use a context-based registration pattern rather than per-page header buttons or `window.location.reload()`: a `PageRefreshProvider` wraps the layout, each screen registers its own data-refresh handler (the existing SWR `mutate()` or `loadData()`), and the header renders the action button only while a handler is registered — clicking refetches that page's data without reloading the whole layout; static pages that don't register simply show no button. This was applied uniformly across ~11 dashboard screens (sessions, shifts, reports, inventory, pricing, promotions, tools, staff, cashflow, membership-plans, customers), and replaced both the previous per-page refresh buttons and the full-page reload approach. Confidence: 0.78
 record; stock is deducted once at sale. Confidence: 0.6
- Two-phase delete based on usage count: when deleting an entity that may have related transaction records, call a `countUsage` method first. If 0 related records exist → hard delete (simpler, nothing to preserve). If >0 related records exist → deactivate (`isActive=false`) to preserve audit/history. Each path writes a distinct audit action (`ENTITY_DELETE` vs `ENTITY_DEACTIVATE`) with context explaining why. This complements the existing "always soft-delete members" rule by establishing that **hard delete is acceptable when no data depends on the entity**, while entities with transaction history are deactivated rather than destroyed. Confidence: 0.65
- Use Prisma `_count` with `select` on multiple related collections (e.g., `stockMovements`, `invoiceItems`, `sellItems`) in a single query to determine if an entity can be safely hard-deleted, rather than running separate count queries per relation. Confidence: 0.60
- For chart libraries, prefer an already-installed dependency whose peer-deps are compatible with the current stack (Next + React 19 + Tailwind v4) over installing a new one with peer-dep conflicts — e.g., when the user asked for `tremor`, the agent verified `@tremor/react@3.18.7` only supports `peerDependencies: react: '^18.0.0'` (rejects React 19, would need `--force`), and pivoted to `recharts@3.10.1` (already in `package.json`, peer `^16.8 || 17 || 18 || 19`) which the user accepted. Concrete steps before recommending a chart library: (1) check the project's actual React/Next/Tailwind versions, (2) `npm view <pkg> peerDependencies` to verify the requested dep is actually compatible, (3) grep `node_modules/<pkg>/package.json` to see if it's already installed, (4) if it's already installed and compatible, recommend it over adding a new dep. Don't ship `--force` installs that compromise the project just to satisfy a "use X library" request. Confidence: 0.85
- When using recharts 3.x tooltips, treat the `content` prop's `payload` as `unknown` and validate inside the render helper rather than typing it via `TooltipProps<number, string>` — recharts 3.x's `TooltipPayload` type is structurally complex (nested `payload`/`dataKey`/`name`/`value`/`color` properties with their own constraints) and direct typing causes `TooltipPayload[]` assignment errors. Concrete pattern: `function renderMoneyTooltip(active, payload: unknown, label, ...) { if (!Array.isArray(payload) || payload.length === 0) return null; const entries = payload as TooltipEntry[]; ... }` with a local `interface TooltipEntry { name?, dataKey?, value?, payload?, color? }` for the cast target. Recharts' `content` prop callback signature is `({ active, payload, label }) => JSX`, so unpack the destructured args before passing to the render helper. Confidence: 0.7
- When removing a feature/page entirely (not relocating it to a new path), clean up all traces across the codebase in one pass: delete the route/page file and its directory, remove navigation entries from sidebar and bottom-nav, remove title/route-key mappings from headers, and remove any now-unused imports (e.g., icon components) from layout files — do not leave orphaned references that will cause stale build-cache or resolution errors. Confidence: 0.70
- When the user asks to restrict what a role can access (e.g., "Tôi không muốn manager được access như admin. Chỉ được access Ca làm, Hội viên, Báo cáo, Kho."), they mean full-stack enforcement, not just hiding menus — when offered "Chặn hoàn toàn" (block at API and UI) vs "Chỉ sửa navigation" (UI-only), they chose blocking completely. A role restriction must be enforced at every layer: API routes (server role checks), use-cases, server-side page guards (redirect), AND UI (sidebar, bottom-nav, screen shortcuts, action buttons), so the restricted role can neither reach the screen nor call the endpoint. This is a recurring pattern the user keeps applying iteratively to narrow MANAGER's access to operational-only (first blocked admin screens, then removed Báo cáo entirely with a terse "remove Báo cáo from manager"): the agent enforces it at every layer without needing per-layer confirmation — bottom-nav tab count per role, page guard redirect, all API routes of the module switched to admin-only, sidebar `adminOnly` flag, and the "Lối tắt vận hành" shortcuts in more-screen — plus cleans up the now-redundant role flags in the affected screens. The same full-stack treatment applies in the opposite direction when restricting STAFF: user asked "Giới hạn quyền staff truy cập shifts và bỏ navigation" — the agent added a server page guard on `/shifts` (STAFF redirected to `/sessions`), guarded `GET /api/shifts` server-side so STAFF keeps only the POS operational queries (`current`/`openOperational`) and list/groupBy queries return 403, and removed navigation for STAFF in one pass (sidebar `staffHidden` on "Ca làm", more-screen "Lối tắt vận hành" shortcut filtered to MANAGER/ADMIN) — access blocking and navigation removal always go together, still without per-layer confirmation. Confidence: 0.85
- For codebase-wide value changes (e.g., a full re-theme of colors), measure the blast radius first with frequency counts (`grep -ohE` the class/value pattern over `src` | `sort | uniq -c | sort -rn`) before choosing a strategy; when the count is in the thousands (~2,500 hardcoded Tailwind color classes), reject per-file hand migration as infeasible and error-prone and instead remap at the definition layer (Tailwind `@theme` color scales + the CSS variables in `globals.css`) so every existing utility inherits the new values with zero component edits — then re-grep for the small residual outliers (off-scale color families, inline hex in charts/SVG) and fix only those. Prefer the single choke point over the exhaustive migration whenever one exists. Confidence: 0.7
- When the user asks a terse architecture question framed as "có thực sự cần X không? và liệu Y có tốn Z (free tier) không?" (e.g., "Để cập nhật realtime có thực sự cần websocket và liệu phương pháp này có tốn vercel free không?"), they want a structured runtime/infra analysis — not code changes — covering: (1) whether the technical primitive is actually required for their domain (define "realtime" tiers, judge business need), (2) what the chosen free tier actually supports and what it does NOT support (Vercel Hobby: 30s SSE timeout OK, native WebSocket not supported, no Cron/background on free, function execution time limits), (3) a tiered table of concrete options (pure polling / SWR polling / SSE / managed WS) each with Vercel-Free cost estimate, pros/cons, and concrete request-count math, and (4) a single recommended path sized to the actual business need (e.g., 5-10 staff, POS shift length) — followed by waiting for the user to pick before implementing. The user does not want the implementation to start inside the answer; the question is investigative ("phân tích từ phía runtime/infra") and the answer should end with 2-3 concrete next-step options for the user to choose. Confidence: 0.7
- React render purity is enforced by this project's ESLint config: impure calls like `Date.now()` are not allowed during render. For UI that must tick (live per-player play/pause timers in the checkout drawer), drive it from a `useState` timestamp updated by `setInterval` in a `useEffect` (skipped when the time is frozen via `frozenAt`, cleaned up on unmount), and derive all durations from that tick value — never from `Date.now()` inline. Related: pass already-formatted display strings (e.g., `PickerMemberStat.playedText`/`pausedText` = "hh:mm") from the parent into purely presentational child components instead of raw seconds plus a formatter living in the child, so time formatting stays in one place and the child stays render-pure. Confidence: 0.7
- For user-initiated mutations (pause/resume, check-in, etc.) on a screen with a clear optimistic state shape (e.g., `pausedAt` flip), apply local state BEFORE the network call instead of waiting for the API roundtrip, then reconcile on the response: the user is highly sensitive to UI latency and reports any ~1s delay between click and visual change as a real bug ("Tại sao khi tôi ấn dừng thì phải mất một khoảng tầm 1 giây mới react?"). The standard pattern: capture `previousX` snapshot, set the optimistic value, `await` the API, on success reconcile with server-truth (e.g., replace optimistic `pausedSeconds` estimate with the real value returned by the server), on failure revert to the snapshot. Apply uniformly to all symmetric handlers in the same screen (e.g., all 4 of `handlePause` / `handleResume` / `handlePausePlayer` / `handleResumePlayer`), and also disable the trigger button while in-flight (e.g., add `submitting` to the button's `disabled` prop) so the user can't double-click during the network window. Confidence: 0.85
- When the user asks to apply a UI-pattern broadly across a feature ("Aplly optimistic update cho tất cả chức năng có trong feature 'ca hôm nay'"), do NOT apply it uniformly to every handler — first classify each handler by what it touches, and refuse optimistic UI for any handler that mutates money, inventory, audit history, or creates persistent financial records (e.g., checkout that creates invoice + payment, sell that creates invoice items and deducts stock, open/close shift that writes expected cash, tool count that writes ShiftTool). The user agrees with this scoping when presented as a structured `ask_user_question` with the "only pure-state interactions" option labeled `(Recommended)` and the rationale that money/inventory ops risk ledger drift if a rollback fails. Only operations whose revert is fully client-side safe (session/player state, pure display fields like `name`) get the optimistic treatment. End with a table classifying each handler as "applied / skipped + reason" so the scope decision is auditable. Confidence: 0.85
- When the same dialog/form is needed on a second screen (e.g., `RenewMemberDialog` previously lived only in the member list, but the renew action got moved to the customer detail page), extract it to its own file (e.g., `renew-member-dialog.tsx`) with a minimal input shape (`RenewMemberInput` = `id` + `fullName` + `membershipStatus` + `currentMembership?`) and export it from there, then have both screens import the shared component — don't duplicate the dialog inline, don't re-export it from a barrel that pulls in the original screen's other unused code, and update the original screen's local-state type to the new minimal input type (e.g., `useState<RenewMemberInput | null>`) so the table/drawer sites have to construct the input explicitly. The parent still owns `submitting` + `setSubmitting` + the post-action `onDone` callback so each screen controls its own refresh behavior after the shared dialog closes. Confidence: 0.8
- For synchronous reads from a browser API store (e.g., `window.matchMedia('(prefers-color-scheme: dark)')` for theme-aware chart colors, `navigator.onLine` for connectivity), use `useSyncExternalStore` with three arguments: a `subscribe(callback)` that wires the change listener and returns its cleanup, a `getSnapshot()` that returns the current value (must be stable — same input → same output), and a `getServerSnapshot()` that returns a safe default for SSR (e.g., `() => false` when the client value would mismatch the server). This is the correct pattern over `useState` + `useEffect` for one-shot reads because: (1) it avoids the `react-hooks/purity` lint error from `Date.now()`/`window` access in render and the `react-hooks/set-state-in-effect` lint error from updating state in an effect, (2) it re-renders correctly on change without manual subscription bookkeeping, and (3) the SSR snapshot is explicit, so there's no hydration mismatch. Used in `reports-charts.tsx` to subscribe to color-scheme changes for chart axis text colors. Confidence: 0.75
