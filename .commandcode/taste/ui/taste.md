# ui
- Use a blocking overlay (modal/backdrop) when no shift is open instead of only disabling buttons and showing warning banners. The overlay must cover only the content area, leaving both the bottom navigation (mobile) and sidebar navigation (desktop) accessible. Confidence: 0.82
- On mobile, hide the sidebar navigation and logout button; they should only display on desktop ratio. Confidence: 0.65
- When a desired UI component (e.g., Switch) doesn't exist in the component library, use a native HTML element with Tailwind styling instead of creating a new component. Confidence: 0.75
- When displaying data grouped by day, sort today's group to the top of the list. Confidence: 0.85
- Transaction/invoice lists must be sorted most-recent-first (descending by payment/creation time), enforced at the data layer (`orderBy: { paidAt: 'desc' }` + JS sort) so every consuming surface inherits the order — the user explicitly asked "Sửa các thứ tự danh sách các giao dịch, hóa đơn theo thứ tự gần đây nhất". When implementing, audit ALL list sources for consistency (shift transactions, customer invoice history, recent payments, cashflow, membership history, session lists, CSV/revenue exports) rather than just the reported screen, and update any test that asserts the old ascending order. Confidence: 0.8
- When functionality moves to a dedicated module/page, remove the old tab and replace it with a navigation link to the new location rather than keeping both. Confidence: 0.80
- When moving an action button from one location (e.g., table action column, drawer footer) to another (e.g., the detail page header), remove it from EVERY former location in the same pass — table column, drawer footer, and any inline rendering — not just the one mentioned. After moving, the detail page hosts the action as an additional button in the existing header action cluster next to "Chỉnh sửa"/"Xoá" with the same `variant="inverse"` style as the original. The button stays disabled (and the row stays non-clickable for it) when the prerequisite isn't met (e.g., no open shift → disabled; for new members with no membership history → "NONE" status → disabled too, since there's nothing to extend). After removal, clean up now-dead code: unused callbacks (`renderRenewButton`), unused props on drawers (`renewDisabled`, `onRenew`), and any imports/types left over from the removed button. The user phrased this as "chuyển nút Đóng tiếp/Gia hạn vào trong trang Cài đặt" and expected the original locations to be empty afterward, not to still host a hidden or duplicate copy. Confidence: 0.8
- Elevate key contextual information (e.g., customer identity) to the page header rather than leaving it buried in a content-column detail card — the user moved the "Khách hàng" card from inside the right column of `InvoiceDetailContent` into the header of `TransactionDetailScreen` ("Chuyển thẻ khách hàng vào header, đặt là Khách hàng: [Tên]") so the customer name is immediately visible on page arrival. The header placement is **inline in the same flex row as the back button**, not on its own separate line below the header — the customer info lives in a left-side cluster `(back button, customer name)` sharing the same `justify-between` row with action buttons on the right. Uses a compact `flex items-center gap-2` with a state-appropriate icon (e.g., `User`) and `Khách hàng: [Tên]` in secondary text color, placing the entity type distinction (member fullName vs. session/customerName for walk-ins) inline in the same label rather than in a separate badge. When the original location still renders the same card (e.g., the right column still shows a `SpineCard`), it stays; the header addition is an elevation, not a relocation that requires removing the old copy. Confidence: 0.8
- When consolidating multiple metadata fields (customer, session, staff) into the component header: render them as a single compact metadata row (with icons + inline labels) positioned below the primary header line (e.g., invoice number + date) rather than in a separate column or multiple separate header lines. Remove the old column-based rendering (e.g., `SpineCard` aside in `InvoiceDetailContent`) entirely — consolidation means one location, not two. The user applied this to `InvoiceDetailContent`'s header: invoice number + status badge on line 1, payment date on line 2, then a single metadata row (`Khách hàng · Phiên · Nhân viên`) as a `border-t` separator below — and removed the entire `<aside>` of `SpineCard` components. Confidence: 0.8
- When a UI element is explicitly moved to a new location, the user expects the old copy to be removed — not kept as a fallback alongside the new one. After initially adding "Khách hàng" to the `TransactionDetailScreen` header as a separate element, the user corrected: "Bạn chưa đặt Đúng vào header của nó" (not in the right place), then "Chuyển tên Khách hàng -> hiển thị dưới ngày thanh toán hóa đơn. Loại bỏ thẻ tên cũ." (move below payment date, remove the old card). The implication: when consolidating/redesigning, the old copy is explicitly deleted, not preserved "just in case." Confidence: 0.8
- When consolidating multiple metadata fields (customer, session, staff) into the component header: render them as a single compact metadata row (with icons + inline labels) positioned below the primary header line (e.g., invoice number + date) rather than in a separate column or multiple separate header lines. Remove the old column-based rendering (e.g., `SpineCard` aside in `InvoiceDetailContent`) entirely — consolidation means one location, not two. The user applied this to `InvoiceDetailContent`'s header: invoice number + status badge on line 1, payment date on line 2, then a single metadata row (`Khách hàng · Phiên · Nhân viên`) as a `border-t` separator below — and removed the entire `<aside>` of `SpineCard` components. Confidence: 0.8
- Destructive or significant actions should prompt for user confirmation via a dialog before execution, rather than executing immediately. Confidence: 0.80
- Use the `useToast` hook with a destructure pattern (`const { success: notifySuccess, error: notifyError } = useToast()`) and call with a plain message string for success/error feedback. Confidence: 0.75
- Render different action buttons based on entity status/state (e.g., show "Void" for PAID records, "Delete" for DRAFT records), hiding inapplicable actions rather than allowing invalid transitions. Confidence: 0.70
- For flex layout overflow issues where adjacent dynamic content (e.g., long employee names) compresses or pushes fixed elements out of view: apply `min-w-0 flex-1` to the flexible content container and wrap the fixed-size element group (e.g., buttons) in a `shrink-0` container. Confidence: 0.85
- For action button clusters in narrow/mobile layouts, let the cluster wrap onto its own line instead of forcing a single row: use `flex flex-wrap` on the outer header row and `flex shrink-0 flex-wrap` on the button group so buttons drop below the info text on mobile rather than overflowing and being clipped by a parent's `overflow-hidden` — interactive controls AND information must never be cut off on mobile (user reported the "Tham gia ca làm" button being hidden on mobile, and later reported ShiftRail losing info + buttons to `overflow-hidden` on mobile). When a component is reported broken this way, apply a comprehensive responsive fix across the whole component ("sửa lại UI responsive tổng thể") rather than patching a single element; keep the desktop layout unchanged with buttons still horizontal to the right. A proven implementation for a 4-button cluster (from the ShiftRail rewrite) is to restructure the component vertically — status header with a state Badge, info lines, then a separate button row — and lay the buttons out as `grid grid-cols-2 gap-2 md:flex md:flex-wrap md:items-center md:justify-end md:gap-2`, with the no-shift primary action `fullWidth` on mobile; also drop the parent `overflow-hidden`/`truncate` that caused clipping. Confidence: 0.8
- Prefer letting long text content wrap to multiple lines (`break-words`, `flex-wrap`) to display fully rather than truncating with `truncate`, even in constrained flex containers; truncation was tried for long employee names but the user explicitly asked to instead wrap/xuống dòng so information is shown in full. Confidence: 0.60
- Prefers mobile-friendly card-list patterns for entity lists: rounded cards with touch-press feedback (`active:bg-*`), compact spacing (`space-y-2 p-3`), and loading/empty skeleton states — rather than traditional data tables — especially for interfaces primarily used on mobile. Confidence: 0.70
- When creating an alternative view component for the same data (e.g., CardList as an alternative to Table), design it with the same column/sorting API (`columns[]`, `keyExtractor`, `sortableKeys`, `defaultSortKey`, `onRowClick`, etc.) so the components can be swapped without rewriting consumer column definitions. Confidence: 0.75
- Mobile card-list views should support sorting via chip/pill toggle controls (sortable keys rendered as rounded chips with active/inactive states and direction arrows), matching the sorting capabilities of their table counterparts rather than being display-only. Confidence: 0.70
- Card-list items should follow a 2×2 grid layout (`grid grid-cols-2`): top-left = title, top-right = action buttons, bottom-left = detail attributes (stacked vertically, `text-xs`), bottom-right = footer (e.g., expiry date, totals). Each of the four cells is independent; actions and footer each get their own `stopPropagation` wrapper. The user explicitly rejected a top-row-split + full-width-details-below layout in favor of this 4-cell grid. Confidence: 0.85
- When building shared/reusable components, expose configurable props (e.g., `headingLevel`) with sensible defaults so the component adapts to different page contexts without hardcoding assumptions about its surroundings — the user explicitly prioritizes "dễ dàng reuseable" design. Confidence: 0.75
- For responsive data views that swap between table (desktop) and card-list (mobile), render both components simultaneously in the DOM and toggle visibility via CSS breakpoints (`md:hidden` / `hidden md:block`) rather than using JS-based `matchMedia`/`useMediaQuery` hooks. This avoids hydration mismatch and visual flash since both components are always present in the DOM tree. Confidence: 0.80
- For dashboard/reports pages that mix a primary monitor focal (revenue/charts) with secondary analysis (donuts/breakdown/payments) plus task actions (export), prefer a 2/3 + 1/3 column layout at `md+` breakpoint with a single full-width context control (e.g., the range/filter bar) sitting above the columns: main column (`md:col-span-2`) = the focal flow (hero scoreboard + hero chart + primary volume chart), side column (`md:col-span-1`) = the analysis + actions (donuts stacked, hourly/breakdown, recent items, export widget). Mobile collapses to a single stacked column. Concretely: `md:grid-cols-3` outer grid with `md:col-span-2` and `md:col-span-1` children, each side a `space-y-4` stack. The 2/3 ratio gives the focal flow enough horizontal space to render a wide chart without competing with secondary widgets; the 1/3 side keeps secondary analysis always visible but visually subordinate. The user explicitly asked for "Đặt layout 2/3 và 1/3 col cho trang này" on the reports page, applied as `grid grid-cols-1 gap-4 md:grid-cols-3` with the main container `md:col-span-2` and the side container `md:col-span-1`. Confidence: 0.7
- For chart visualizations whose meaningful granularity depends on the time-range filter (e.g., reports page), swap the chart type based on the active range rather than always rendering the same chart: when the range covers a single calendar day (`from === to`), render a finer-granularity bar/line chart of the metric by hour (e.g., `HourlyBarChart`); when the range spans multiple days, render a coarser area/bar chart by day. The title also swaps accordingly ("Doanh thu theo giờ" vs "Doanh thu theo ngày"). The single-day/multi-day check is `isSingleDay(from, to) = from === to` (calendar-day equality, not duration). Backend data sources stay separate: a single-day chart pulls from the hourly aggregate (`trends.byHour`), a multi-day chart pulls from the daily aggregate (`revenue` from `/api/reports/revenue`). Don't show both — pick the one matching the range and hide the other's section entirely. This avoids confusing the user with meaningless aggregation: `byHour` summed over multiple days would mash different days into the same hour bucket and look wrong. Confidence: 0.75
- When a column header or section label already provides semantic context (e.g., "Hạn"), display dates as plain values (e.g., `09/09/2026`) without redundant descriptive prefixes like "Còn hạn đến" or "Hết hạn từ". The surrounding label already conveys the meaning. Confidence: 0.75
- Keep dialogs single-purpose: when a dialog would need to embed a business flow that has its own dedicated screen (e.g., member creation/renewal embedded in the check-in dialog), remove the embedded flow and instead show an explanatory warning plus a button that closes the dialog and routes (`router.push`) to the dedicated tab. The user explicitly requested this ("nhấn tạo hội viên -> chuyển sang tab đăng ký hội viên") and had the embedded "Gia hạn & check-in" flow removed entirely rather than kept with guard fixes. The same single-purpose preference extends to operationally distinct tasks bundled into one dialog: the user asked to split tool counting out of the open-shift dialog ("Tôi muốn tách hai việc mở ca và đếm dụng cụ") — opening a shift keeps only cash + notes, and a separate "Đếm dụng cụ" button + dialog is added while close-shift counting stays as-is. Confidence: 0.85
- When searching for entities whose usability depends on a derived status (e.g., membership validity), validate/annotate at search time: request the status fields from the API (e.g., `includeMembershipStatus=true` → `membershipStatus`/`currentMembership`) and render status badges (Còn hạn / Hết hạn / Chưa đóng) directly in the result list so staff see who is eligible before selecting. The user explicitly asked to "validate hội viên trước khi load lên UI để UX được tốt hơn khi tìm Hội viên". Confidence: 0.7
- Don't `disabled`/block list rows whose state is invalid but has a remediation path (e.g., an expired member that leads to the renewal tab): disabling prevents the user from ever reaching the warning + CTA. Instead show the status badge and keep the row selectable so selecting it reveals the next-step action (agent reverted a `disabled` + `opacity-60` state on expired members for exactly this reason). Confidence: 0.55
- For one-time actions (e.g., counting tools once per shift), once completed, disable the trigger button and rename it to reflect the done state (e.g., 'Đếm dụng cụ' → 'Đã đếm D.cụ') so staff can see at a glance the action is already done — user explicitly requested "Disable nút và đổi tên thành 'Đã đếm D.cụ'". Confidence: 0.7
- When a stored reference value must be reconciled against user-entered values at a later step (e.g., open tool counts vs. close-shift counts), surface the comparison in the dialog as per-item Khớp/Lệch/Chưa nhập indicators plus a running "X/Y dụng cụ khớp" summary so staff see mismatches before submitting, rather than silently accepting the entered values. Confidence: 0.55
- Reserve space for conditionally-rendered secondary content (e.g., a "Đã tạm dừng HH:MM:SS" line under the elapsed timer) even when the data is currently empty — render the element with `invisible` (keeping its dimensions) rather than not rendering it at all, so toggling the data on/off doesn't cause adjacent elements (action buttons, surrounding cards) to shift position. The visible state swaps the `invisible` class plus color/aria attributes; `aria-hidden={!hasData}` keeps the empty row silent for screen readers. Applied to `SessionTimer` after the user reported layout shifting on the active-session card when pause is pressed ("Mỗi thẻ người chơi active cần để chỗ cho dòng Đã tạm dừng timer khi ấn dừng tránh khi nó xuất hiện thì bị layout shifting"). **Refinement:** when the same content is moved to a different location in the same component (e.g., the "Nghỉ" line relocated to under the player name on the left column), the `invisible` reserve in the original location is no longer needed and should be removed — replace it with a proper conditional render (e.g., a `hidePaused` prop that omits the row entirely) rather than keeping the `invisible` placeholder that wastes ~14px of vertical space. Don't hack it by passing `pausedSeconds={0}` to suppress the content; add a real prop so the component's API is clean. The user explicitly said "loại bỏ khoảng trống reserved trước dành cho timer đó" after a refactor moved the "Nghỉ" line elsewhere, expecting the reservation to be deleted (not preserved) since the row's purpose moved. Confidence: 0.85
- For action-heavy rows like the active-session card, prefer a vertical 2-row stacked layout that works the same on mobile and desktop over breakpoint-dependent `md:flex-row` horizontal arrangements: row 1 = identity + meta inline on the LEFT and the live timer (or collapse control for groups) on the RIGHT (`flex items-start justify-between gap-2`, with the left side using `flex min-w-0 flex-wrap items-center gap-x-2 gap-y-1` and the timer button getting `shrink-0`); row 2 = secondary action(s) + primary CTA grouped on a single line, right-aligned (`flex items-center justify-end gap-1.5`). For single-player sessions row 2 contains both Dừng/Chơi (secondary variant) and Thu (inverse variant) side-by-side; for multi-player (group) sessions row 2 only contains Thu because per-player Dừng/Chơi live inside each `PlayerPauseCard`. The user originally asked for a 3-row pattern (meta+identity, timer+pause, Thu on its own line) but later refactored to 2 rows — moving the timer to the right of row 1 ("Okay đưa timer sang bên phải") and consolidating Dừng+Thu onto one line ("các nút Dừng và thu đẩy xuống dưới xếp cạnh nhau"). Meta info (check-in time, tạm tính) belongs inline with the title on row 1's left side, not on its own sub-line — long names push meta down via `flex-wrap`, but it never lives in a separate "metadata row" below the title. The agent dropped the `md:flex-row` breakpoint logic entirely and applies the same 2-row pattern to both breakpoints. Confidence: 0.8
- Numeric quantity inputs should support direct keyboard entry, not only +/- stepper buttons: render an `Input` backed by a raw string state (so it can be typed/cleared) and clamp to min/max on submit — user explicitly requested keyboard entry for the walk-in player count ("Số người chơi nên cho phép thêm nhập số lượng bằng bàn phím"), with the stepper kept as a complement. Use `type="text"` + `inputMode="numeric"` + `pattern="[0-9]*"` + regex validation (and `maxLength`) rather than `type="number"`: React number inputs emit transient intermediate values ("1.", "01") that desync the DOM from state and make the + button jump by 2 (user reported "ấn + thì giá trị bị +2" in the player-count field). Stepper buttons must use functional updates (`setState(current => ...)`) so rapid clicks read the latest state instead of a stale render-scope value, for both + and − buttons. Confidence: 0.7
- The elapsed-time clock is the primary live info on active session cards: shown at the top-right with a Timer icon, icon color matching the entity's badge color (purple for members, emerald for walk-ins), with no contextual status label underneath. The user asked to move the clock there and "làm rõ dễ nhìn", then applied it uniformly to members ("Áp dụng chung cho cả hội viên"). Refined by later feedback: the clock's font size should match the customer-name size, not be oversized ("Giảm cỡ chữ Timer bằng với tên khách" — reduced from text-xl to text-sm, same as the name), no running price/total is displayed at all during play, and the "Chưa chọn bảng giá" contextual label was later removed outright ("Loại bỏ luôn dòng 'chưa chọn bảng giá'") — the user removed the live price logic from the card because price isn't needed from session start through play (see the pricing taste). Confidence: 0.7
- Don't render badges for the default/assumed state: the "Vãng lai" badge was removed from active session cards because every non-member walk-in would show it (pure visual noise) — badge only non-default/notable states such as "Hội viên" (members) and "X người" (groups). The principle generalizes: prefer encoding entity state/role through inline color on the existing label (e.g., tinted the customer name itself — `text-purple-600` for members, `text-amber-600` for paused sessions, default zinc for walk-ins) over appending separate badge chips. The user asked to drop the "Hội viên" and "Tạm dừng" badges entirely and surface those states via the name's text color, with paused taking priority over member when both apply (`isPaused ? amber : isMember ? purple : zinc`). When doing this, also drop the now-unused `Badge` import. When the state-bearing label sits next to an inline action icon (e.g., a pencil "rename" hint on `PlayerPauseCard`), the icon should also adopt the same state color so the label + its affordance read as a single tonal unit rather than the icon clashing with a colored name. Apply the pattern uniformly across sibling components once established on one — the user followed up with "Dọn dẹp tương tự với @src/features/pos/player-pause-card.tsx" immediately after the active-session-card pass, expecting the same badge-removal + color-on-name pattern in every per-player/related card, not just the first. Confidence: 0.85
- Validate time inputs client-side with bounds derived from business context rather than accepting arbitrary values: the user explicitly requested client-side validation ("validate client side") that rejects a check-in/adjust time earlier than the shift's open time and later than current time + 5 hours — i.e., prevent out-of-range time entries in the UI instead of letting them through to fail later. The request was restated and refined to name the check-in time specifically ("không cho phép nhập giờ check-in trước giờ mở ca và sau giờ hiện tại 5 tiếng"), with the check-in dialog receiving the shift open time as a prop and rejecting out-of-bounds selections via error toast before submit; validation only runs when staff deviate from the default (now) value. Confidence: 0.7
- Role-gate destructive actions in the UI: render admin-only buttons (e.g., Xoá hội viên) only when `isAdmin`, and mirror the same restriction server-side in the route handler. Confidence: 0.6
- When a secondary financial amount (e.g., pending-sell total of products already added to a session but not yet paid) needs to be surfaced at a glance, place it inline next to the primary action button as a compact "Tạm tính: {amount}" label rather than in a separate summary section or tooltip — the user explicitly asked to place it "cạnh nút 'Thu'" so staff see the pending total immediately when deciding to collect payment. Confidence: 0.55
- In the checkout drawer, pending sell items (hàng bán kèm chưa thu) render as a flat per-line list (product × quantity + subtotal) with NO grouping by sell event — since bán kèm no longer creates DRAFT/SEL invoices there is nothing to select per event: ALL remaining pending items merge automatically into the single INV invoice at each checkout (each line consumed exactly once). Per-line opt-out is a checkbox that is always checked; unchecking it deletes the line server-side (`DELETE /api/sessions/[id]/sell-items`, which also restores stock) and then reloads the checkout preview, rather than being local UI state. This supersedes both the earlier DRAFT-grouped checkbox design and the interim no-checkbox plain-list version (per docs/check-out.md). Confidence: 0.7
- In compact cards/status rows, drop redundant text labels when an icon already conveys the meaning: keep only the icon plus the value (e.g., active-player card shows just the `LogIn` icon + the check-in time, with the word "Check-in" removed per the user's explicit request "loại bỏ chữ 'check-in', chỉ giữ lại giờ và icon"). Icons act as the label; text should not duplicate them. Confidence: 0.6
- When the user gives a terse visual sizing instruction with no specific target ("Make X bigger", "nhỏ lại", etc.), interpret it as the next step up/down the Tailwind scale and bump accompanying icons proportionally in the same pass — e.g., a terse "Make name and timer bigger" request on `@active-session-card.tsx` / `@player-pause-card.tsx` was resolved as `text-sm` → `text-base` for the label and `text-sm` → `text-base` + icon 15 → 18px for the timer, and the change was propagated to the shared `SessionTimer` subcomponent (one step up, no over-shoot) rather than jumping multiple steps or asking for a specific value. Confidence: 0.55
- Don't show metadata that is already inferable from other visible information on the same row/card — derive-then-strip rather than label-everything. Example: on the active-session card the user asked to drop the "Ca HH:MM" label entirely ("Loại bỏ thông tin Ca làm trên phiên vì chỉ cần nhìn vào giờ check in là có thể biết ca nào rồi"), because the check-in time already implies which shift the session belongs to. The general principle: before adding a piece of metadata to a compact display, check whether it's a 1:1 function of another value the user already sees — if so, the second label is visual noise. This applies even to "nice to have" context like shift/period, region/zone, or status fields that are deterministic consequences of other visible fields. Confidence: 0.7
- Decision-critical sections at payment/checkout (e.g., "Bảng giá", "Khuyến mại giờ chơi", "Phí gửi xe") should always be fully visible rather than collapsible — the user asked to remove the collapse feature ("bỏ tính năng collapsible") for these three so staff see the full pricing breakdown at payment time. (Now generalized: see the flat box-free `Section` rule below — the whole checkout drawer was later stripped of collapse affordances.) Confidence: 0.7
- When a summary section must stay collapsible somewhere, it should default to expanded ("mặc định expand") so its content is visible on load without interaction, and the fixed total + payment CTA stay outside the collapsible area. Note this is now the exception, not the rule: the user's later "Move content out of Boxes" pass deleted the collapse mechanism (chevrons + open state) from the checkout drawer entirely. Confidence: 0.55
- Prefers flat, box-free section layout inside modals/drawers — no framed "card" wrappers for content sections. In the checkout drawer the user asked to "Move content out of Boxes": the bordered/rounded/padded `SectionCard` was replaced by a plain `Section` (semantic `<section>` + `<h4>` title + optional right-aligned `trailing` note), content rendered directly on the surface, adjacent sections separated by a single hairline `border-t border-zinc-200 dark:border-zinc-800` with `first:border-t-0 first:pt-0`. Deleting the box also deletes the collapse affordance — everything renders flat and expanded (a screen reads as a titled list, which also suits mobile fullscreen modals). This supersedes the earlier per-section card/collapse designs. Confidence: 0.8
- On invoice details, when a line item represents a per-person billed service (e.g., play time), display an itemized per-player breakdown — one line per player: `Người 1: 1.4h (Tên bảng giá) = 324.000đ`, `Người 2: 2.3h (Tên bảng giá) = ...` — showing each player's hours (one decimal place), the applied pricing-rule name in parentheses, and the individual total. The user explicitly rejected the earlier aggregated form `45.000đ/người × 2 người` for this case ("Không tôi muốn sửa là thông tin sẽ ghi rõ như sau: Người 1: 1.4h (pricing rule) = 324.000đ"). Persist the per-player breakdown (hours, subtotal, discount, total, pricing rule name) in the line-item metadata at checkout so the invoice can render it, and keep a fallback to the old display for legacy records lacking that metadata. The same itemized breakdown must also appear live on the checkout/collection screen before payment, not only on the post-payment invoice: the user asked "Sửa ở mục check-out đi" to add it to the checkout drawer's summary, which required the checkout-preview quote API to return the same `playerPricing` detail computed with the exact per-player logic as the actual checkout (per-player `calculatePlayerPrice`, bill-count slice order, pricing-rule name from the group snapshot). Confidence: 0.85
- Keep checkout/invoice summary lines minimal: when a full per-player/per-item breakdown is displayed right below, drop redundant derived aggregates from the summary label — the user asked to remove the "x người · bình quân .../người" line entirely, leaving just the simple label "Giờ chơi" (the per-player lines already convey count and individual amounts, so the aggregate was visual noise). Confidence: 0.6
- Modals should use close to the full viewport height and extend toward the bottom edge: the shared `Modal` uses `max-h-[calc(100dvh-4rem)]` on mobile and `md:max-h-[95vh]` on desktop with reduced bottom padding (`pb-8`) so content fills downward — the user asked to "Tăng chiều cao của modal về phía dưới" and the agent applied it to the base component so all modals (including the checkout drawer) benefit. Confidence: 0.6
- Modal header and footer should have a background visually distinct from the body (e.g., `bg-zinc-100/70` light / `bg-zinc-950/60` dark on top of the existing border) so the chrome reads as separate regions from the content — the user asked "làm phần footer và header background nổi hơn để phân biệt được với phần body", and the change was applied in the shared `Modal` base component (not per-instance) so every modal/drawer benefits. Confidence: 0.6
- Prefers minimal UI copy in checkout/payment screens: removes descriptive/instructional helper-text paragraphs under form sections (e.g., "Bảng giá được chọn theo thời điểm thu tiền và ghi lại vào phiên.", "Chọn một khuyến mại còn hiệu lực tại thời điểm thu tiền.") rather than keeping them, and strips select-option labels down to just the entity name (e.g., pricing-table dropdown shows only `rule.name`, no `— {money}/giờ` or `(N bậc luỹ tiến)`); error states (red text) remain, only informational prose and secondary detail are removed. This also extends to abbreviating repetitive per-player labels inside the invoice info so lines stay compact: the user asked to shorten "Người x" → "Ng. x" in the per-player breakdown, applied consistently in both the checkout summary (`checkout-drawer.tsx`) and the invoice detail (`invoice-detail-content.tsx`), and scoped only to the "thông tin hóa đơn" areas (the pause card was intentionally left untouched). The same minimalism applies to status lines: text describing the default/expected collection state is removed outright — the user asked to delete the "Thu hết X người — mỗi nhóm tính theo bảng giá riêng." line shown when collecting everyone (including a single player), leaving only the "Thu trước X/Y người" warning that appears for the non-default partial-collection state. Confidence: 0.75
- After removing the last usage of a helper (e.g., the `money` formatter), delete its now-unused import and any now-dead derived state (e.g., a `todayStr` variable superseded by a weekday check) so the file stays clean. Confidence: 0.65
- Option/label names in checkout/pricing pickers should name the resulting mode directly rather than the underlying mechanism: the user renamed the pricing-table mode options "Cùng một bảng giá" → "Một bảng giá" and "Chia nhóm" → "Nhiều bảng giá" — the label should describe what the user gets (one price table / multiple price tables), not the grouping action behind it. Confidence: 0.55
- To change a shared component's sizing/height, edit the base component's defaults directly rather than passing a per-instance `className` override: in this Tailwind setup, override utilities passed via `className` cannot reliably beat the base component's responsive utilities (e.g., `md:max-h-[90vh]`), because Tailwind orders utilities by their position in the generated CSS, not by className concatenation order — the agent first added `className="md:max-h-[95vh]"` to the checkout drawer but reverted it and changed `Modal`'s defaults for a guaranteed result. Confidence: 0.65
- A modal "fullscreen" variant must mean TOTAL viewport coverage on mobile — zero wrapper padding/margin outside the modal, no rounded corners, no border (a bare `fixed inset-0` wrapper with the content at `h-full w-full`) — any visible gutter between the modal edge and the screen was explicitly rejected ("Chưa đúng lắm, cần nó bao toàn bộ màn hình mà"), while desktop stays the centered padded rounded modal. Implement per-variant styling as mutually-exclusive class branches (e.g., `isFullscreen ? "<full replacement string>" : "<shared base string>"`) instead of appending counter-classes (`p-0`, `rounded-none`, `border-0`) onto a shared base string that contains their opposites (`p-4`, `rounded-2xl`, `border`): when duplicates coexist on one element, stylesheet order — not string order — decides the winner, so counter-classes silently lose. Any constraint that would limit mobile coverage (e.g., the `size` prop's `max-w-*`) must be moved behind a `md:` prefix (`sizeMdClasses` map mirroring `sizeClasses`) so nothing conflicts at mobile and is restored at the desktop breakpoint; after restructuring, verify the desktop look stayed pixel-identical (the intermediate attempt wrongly added inner `md:p-4` and dropped the shadow before being corrected). Same root cause as the className-override rule above, generalized to intra-component variant composition. Confidence: 0.75
- When a JSX ternary chain grows beyond 3 levels, extract it into a named helper function with early returns and descriptive condition names — the agent replaced a 7-deep nested ternary on the CTA button label with a `getCtaLabel()` function. Confidence: 0.75
- Extract repeated Tailwind button class strings into named `const` values at the top of the file (e.g., `const stepperMinus = "flex h-11 w-11 items-center ..."`) rather than duplicating inline classes across multiple button instances — this made touch-target size changes a single-line edit instead of a multi-instance grep-and-replace. Confidence: 0.7
- Modals/drawers must implement focus management: trap Tab inside the modal (cycle focus between first and last focusable element), auto-focus the first interactive element on open, and restore focus to the previously-focused element on close. Implemented via a `useRef` on the content div + a `useEffect` that queries `focusableSelector` and handles `keydown` Tab/Shift+Tab. Confidence: 0.7
- Loading skeletons must structurally mirror the real layout they replace (header shape, stats grid shape, action-bar shape, section list shape) so the page doesn't visibly "jump" when data arrives — applied to `TodayShiftSkeleton` to match the new `ShiftRail` composition (header 1 line, stats 3-col with the accent stat taller, action bar 2-col mobile / 4-btn desktop, sessions section with header + 2 list rows). A faithful skeleton is part of the layout, not a generic pulsing block; after a structural change to a real component, update its skeleton in the same pass. Use alpha-tinted variants of the real colors (e.g., `bg-emerald-500/40` for a "loading" version of the emerald status bar) to preserve the dominant color signal during the loading state. Confidence: 0.8
- Project has an established motion system: keyframes live in `src/app/globals.css` (`fade-in`, `slide-up`, `slide-down`, `scale-in`, `slide-in-right`, `shimmer`, `toast-pop`, `card-enter`) and a `prefers-reduced-motion` global kill-switch sets every `animation-duration` / `transition-duration` to `0.01ms` at the end of the file. New motion work should reuse these existing keyframes (`animate-fade-in`, `animate-slide-up`, `animate-slide-down`, `animate-card-enter`, `animate-[shimmer_1.5s_ease-in-out_infinite]`) rather than inventing new ones, and rely on the global `prefers-reduced-motion` rule for accessibility — no per-component `motion-safe:` / `motion-reduce:` overrides needed unless the global is insufficient. The `Skeleton` primitive uses `animate-[shimmer_1.5s_ease-in-out_infinite]`, `Modal` uses `animate-fade-in` (overlay) + `animate-slide-up` (panel), `ActiveSessionCard` list items use `animate-card-enter` with `animationDelay: index * 30ms` capped at 8. Confidence: 0.8
- When shipping a coordinated "motion pass" across a screen, prefer editing shared primitives (Button, Modal, Skeleton, EmptyState, NoticeCard) once so every consumer benefits — e.g., adding `active:scale-[0.97] motion-safe:transition-transform disabled:active:scale-100` to the `Button` primitive gives ~60 POS buttons tactile press feedback in one line; adding `animate-fade-in` to `EmptyState` and `animate-slide-down` to `NoticeCard` covers all callers without per-instance edits. Pair primitive edits with per-screen callouts (stagger arrival on `TodayShiftScreen`, smooth collapse on `ShiftRail`, chevron rotation on `ActiveSessionCard`) so primitive-level motion + screen-level motion combine coherently. The `disabled:active:scale-100` clause is important: without it, disabled buttons would still react visually to press, which lies about state. Confidence: 0.75
- For chevron toggle buttons (collapse/expand), render a SINGLE icon and rotate it 180° on state change rather than swapping between two different icons (`<ChevronDown />` ↔ `<ChevronUp />`) — apply `transition-transform duration-200` so the rotation is smooth, and use `rotate-180` (or no rotation) class conditionally. This pattern is reused in both `ShiftRail` (overall shift section) and `ActiveSessionCard` (group players expand) — pick one and apply uniformly. The `ChevronUp` import can be deleted once the swap is removed. Confidence: 0.75
- For collapse/expand containers, prefer animating `grid-template-rows: 0fr → 1fr` with a sibling `opacity 0 → 1` on the same `transition-[grid-template-rows,opacity] duration-200 ease-out` over conditional `{!collapsed && <content/>}` unmounts — the unmount approach is jarring (content disappears instantly) and breaks any internal state. Wrap the content in an inner `overflow-hidden` div so the row collapse clips during the transition. Add `aria-expanded` to the trigger button and `aria-hidden` to the collapsible content so screen readers follow the visual state. `ActiveSessionCard` already used this pattern; `ShiftRail` was rewritten to match — apply uniformly across all collapse/expand surfaces. Confidence: 0.75
- For crossfading state-driven color (e.g., session name color flipping zinc → purple → amber on pause/member state), add `transition-colors duration-200` to the element with the state-conditional class — the color change is then smooth rather than snapping. Keep the state-precedence ternary consistent across sibling components (`isPaused ? amber : isMember ? purple : zinc` — paused wins over member) so the same entity type renders the same way in every surface (`ActiveSessionCard` and `PlayerPauseCard` were both updated with the same pattern). Confidence: 0.7
- For page-arrival motion, stagger top-level sections with the existing `animate-slide-up` keyframe plus a `style={{ animationDelay: '<n>ms' }}` per section (e.g., 0ms / 40ms / 80ms) so the sections reveal in reading order. Don't over-stagger — 3-4 top-level sections is enough; deep stagger feels slow. Anchor critical-modal entrances to the same `animate-slide-up` (e.g., the "Chưa mở ca" full-screen overlay) so the modal arrival matches the page rhythm. Confidence: 0.7
- For status/operate boards (e.g., `ShiftRail` showing the open shift), pick ONE primary stat to make dominant — visually bigger (e.g., `text-2xl font-bold` vs the others' `text-lg font-semibold`), tinted with the entity's accent color (emerald for "open", purple for member, etc.), and optionally framed in a tinted card (`rounded-lg border border-emerald-200 bg-emerald-50 dark:border-emerald-500/20 dark:bg-emerald-500/10`). The remaining stats stay smaller and neutral. The dominant stat is the one staff glance for first to decide their next action — e.g., "Đang chơi" (active session count) on the shift rail tells staff whether to take more walk-ins; making it the focal point via a dedicated `variant="accent"` prop on `MiniStat` (rather than ad-hoc styling) keeps the pattern reusable for future status boards. Confidence: 0.75
- On status/operate boards, put numeric stats ABOVE the action bar (reading-then-tapping order: glance stats, then act), and the action bar at the bottom for thumb-reach on mobile. Action bar uses `grid grid-cols-2 gap-2 md:flex md:flex-wrap md:items-center md:justify-end md:gap-2`. The destructive action (e.g., "Đóng ca") stays LAST in the action order so it's furthest from the dominant tap target. Confidence: 0.7
- On status/operate board headers, drop redundant badges that duplicate a color-signal already present elsewhere (e.g., a "Đang mở" green badge is redundant when a left-side colored bar already signals the same state in emerald) — keep the header to icon + title + chevron only. Use a dedicated left-side colored bar (`grid grid-cols-[8px_1fr]` outer, `<div className={shift ? 'bg-emerald-500' : 'bg-amber-500'} aria-hidden />` inner) as the single status signal — 8px is enough width to be a glanceable dominant signal (wider than 6px which is too thin). Mark it `aria-hidden` since it's purely visual. Confidence: 0.7
- Long meta lines that risk mobile truncation (e.g., "Thứ Tư, 28/08/2025 · Tiền đầu ca 500.000đ") should be split into separate `<p>` rows when they combine independent facts (date vs. amount) — each row gets its own line and the second fact can carry extra emphasis (e.g., `font-semibold tabular-nums` on the money value so it reads as a focal sub-point). Don't shrink the text or truncate; just split and let the line height grow. Confidence: 0.65
- Decorative checkboxes (those rendered read-only inside a clickable parent `<button>`) must use `disabled` rather than `readOnly` — `readOnly` still allows Tab focus on the checkbox, creating a confusing double-focus-target where the checkbox receives focus but does nothing; `disabled` removes it from the tab order entirely. Confidence: 0.7
- iOS Safari auto-zooms on input focus when font-size is below 16px — set the shared `Input`/`Select`/`Textarea` base classes to `text-base sm:text-sm` (16px mobile, 14px at `sm:` breakpoint) to prevent this. Applied to the shared `baseInputClasses` in `input.tsx` so all form elements across the app benefit. Confidence: 0.75
- Modal padding should be responsive: header, body, and footer use `px-4 py-3 sm:px-5 sm:py-4` (tighter on mobile to recover horizontal space, standard on desktop). Applied to the shared `Modal` base component. Confidence: 0.65
- Avoid nested cards/borders inside already-bordered containers: when a SectionCard (or any bordered wrapper) contains sub-blocks that also have `rounded-* border bg-*`, flatten them to plain content — double-borders and double-padding waste vertical space. The user explicitly asked to "loại bỏ bớt nested block hoặc card" to recover space for content. Use the outer container's border as the single visual boundary; inner elements get at most `space-y-*` spacing. Confidence: 0.75
- On mobile, keep the top header minimal: show only the logo plus the current page title (derived from the route/pathname), and drop the brand wordmark text (e.g., "VICTORIA / Archery") as well as any supplementary/eyebrow lines that sat above page titles (e.g., "Đối soát vận hành"). The page-level `<h1>` title is then hidden on mobile (`hidden md:block`) so the header title is the single source of truth and the title isn't duplicated in the page body; the desktop layout keeps its own titles and sidebar unchanged. The user asked for exactly this on mobile ("bỏ tên VICTORIA archery, giữ lại logo... chuyển tiêu đề của trang đó... lên [header]") and it was applied uniformly to every dashboard screen. The same consolidation extends to page-level actions: the shared mobile header becomes the single home for common actions like refresh (placed at the right side, `variant="ghost"` rounded icon button), and each page's own refresh button is hidden on mobile (`hidden md:inline-flex`, desktop unchanged) so actions aren't duplicated on mobile. The mobile header title should be sized up as large as possible without exceeding the header height — the user asked to "Cho tiêu đề header mobile to lên nhưng không to hơn header height" (implemented as `text-[17px]` while keeping `py-2.5` and a h-7 logo so total header height is unchanged). Confidence: 0.7
- Stat/summary cards on mobile should be a single compact 4-column row (`grid grid-cols-4`) rather than a 2×2 grid — the user asked to change the inventory stats cards from `grid-cols-2 md:grid-cols-4` to `grid-cols-4` so all four cards sit on one line even on mobile ("đặt các thẻ thành 4 col thay vì 2x2 grid ở giao diện mobile"). Confidence: 0.65
- Conditionally-shown images (e.g., a QR code revealed only when the Transfer payment option is selected at checkout) should use native lazy loading (`loading="lazy"`) so the asset is not fetched until it actually becomes visible — the user explicitly asked to "Make it lazy load" for the transfer QR code. Confidence: 0.7
- In this project, render images from `/public` with a native `<img>` element (with the eslint-disable comment for `@next/next/no-img-element`) rather than introducing `next/image`: the codebase has no `next/image` usage yet, and native img avoids CSP/remote-domain config issues — the agent surveyed existing image references (`/images/` and `from "next/image"` greps) before choosing this to match existing patterns. Confidence: 0.55
- Destructive-action confirmation dialogs should explain the specific consequence (e.g., hard delete vs. deactivate) with a notice/info card that varies its message and tone based on the entity's state, rather than a generic "are you sure?" prompt — staff need to understand the outcome before confirming. The dialog's confirm button label should also adapt to match the actual action (e.g., "Ngưng bán" for deactivation vs "Xóa" for hard delete), and the notice card should use a warning tone for deactivation and info tone for permanent deletion. Confidence: 0.60
- Prefers inline editing over separate edit/preview views: when modifying content (e.g., pricing tables, promotions, invoice lines), edits should happen inline directly within the current context rather than navigating to a separate edit screen, opening a modal, or switching to a distinct edit mode — the user explicitly stated "chỉnh sửa inline luôn" (edit inline, always). Confidence: 0.85
- Merges related information-preview sections with their corresponding editing controls into a single consolidated section rather than keeping them as separate collapsible/sectioned areas — the user asked to "gộp preview invoice vào vùng edit giá giờ và khuyến mãi" (merge the invoice preview into the price-time and promotion edit area), indicating that when a display/preview and its edit controls are conceptually coupled, they should be combined into one section. Confidence: 0.80
- Prefers a switcher/toggle UI control to toggle between edit and preview/invoice modes within the same view rather than navigating to separate screens or keeping both views always-merged — the user asked to "add switcher to switch to preview invoice mode," signaling that when two modes (edit vs. preview) need to coexist, a toggle is the preferred mechanism. Confidence: 0.80
- Prefers unnested/flat modal content structure: information content should live directly in the modal body rather than being nested inside the footer or inside deeply nested collapsible sections; the footer should be reserved for the pay zone/CTA only — the user asked to "unnest information in the modal," moving the invoice summary out of the footer into the body. Confidence: 0.75
- Never build Tailwind utility class names via runtime string interpolation (e.g., `` `grid-cols-${n}` ``): Tailwind's JIT only generates classes that appear literally in source, so dynamically-composed classes (e.g., `grid-cols-5`) silently produce no CSS — the user hit this when the bottom nav displayed wrong by role (ADMIN/MANAGER's 5-tab grid broke because `grid-cols-5` didn't exist anywhere in source, while STAFF's 3-tab variant accidentally worked because `grid-cols-3` was used elsewhere). Fix with inline styles for computed layout values (e.g., `style={{ gridTemplateColumns: \`repeat(${n}, minmax(0, 1fr))\` }}`) or a static class map, and after fixing grep for the same interpolation anti-pattern (`grid-cols-${`, `col-span-${`, `w-${`, `h-${`) to catch all remaining occurrences. Confidence: 0.8
- When fixing role-based navigation/visibility, reconcile the nav's role filter against the actual server-side route guards (e.g., pages that redirect STAFF back to /sessions) before changing behavior — the correct tab set is whatever the server actually permits for each role (STAFF vs ADMIN/MANAGER), verified by reading the guards, not assumed from the nav code alone. Confidence: 0.6
- Don't apply expandable/collapsible group patterns to single-entity cases: if a card row represents one item (e.g., a single-player session) it should render flat with its controls directly, not behind a "Mở/Thu gọn" toggle that exists only to reveal one child — the user explicitly called this out as "rất rườm rà" (very cumbersome/wasteful) and asked the design be split so 1-entity cases render straight on the card while multi-entity cases keep the expandable group. Concretely: a `hasPlayers` flag that was true for both 1-player and N-player sessions was tightened to `isGroup && (rows exist)`, and the per-row `PlayerPauseCard` block (plus its collapse/expand toggle and collapse animation) was removed from the 1-player path entirely. The general principle: branching UI affordances (expand, collapse, tabs, sub-cards) should only be rendered when the variant they're meant for actually needs them — not as a shared container reused for all sizes. Confidence: 0.85
- When a screen's actions split across roles (e.g., MANAGER keeps CSV export of a shift report but loses the adjust-cash-difference edit, or keeps member management but loses the membership-plans link), gate each action with its own capability flag (e.g., `canExport` vs `isAdmin`/`canEdit` props) rather than a single coarse `isAdmin` boolean — so a manager still sees and uses the operations they're allowed while admin-only actions disappear. Confidence: 0.6
- Every UI color must come from the token system in `src/app/globals.css`: semantic tokens (`--color-surface-primary/secondary/tertiary/elevated/overlay`, `--color-text-primary/secondary/tertiary/inverse`, `--color-border-default/strong/subtle`, `--color-brand*`, `--color-gold*`, `--color-success|warning|danger|info` each with `-bg`/`-border`, `--color-accent-purple*`, `--color-focus-ring`, plus shadow/radius/layout vars), exposed to Tailwind through `@theme inline` and defined for BOTH `:root` (light) and `.dark`. No theme may ship a token that the other one lacks. Confidence: 0.75
- When a single UI element encodes multiple overlapping states via color/text (e.g., a session name that's both "paused" AND "member"), state precedence must be explicit: pick which state wins, document the rule in the implementation, and surface it to the user so they can override. Example: the active-session card uses `isPaused ? amber : isMember ? purple : zinc` — paused wins over member because the user said "tên hội viên có màu purple" but only when not paused, and the agent explicitly called out the precedence so the user could flip it. General principle: when two or more states can co-occur on the same element, state priority is itself a design decision worth confirming, not a detail to assume. Confidence: 0.7
- Never hardcode hex literals in components — including chart/SVG series (gradient `stopColor`, `stroke`, `fill`, and color lookup maps like `itemColors`/`methodColors`): reference the runtime CSS variables (`var(--color-success)`, `var(--color-blue-500)`) so charts re-theme with light/dark instead of drifting off-palette. Confidence: 0.7
- Keep exactly ONE canonical set of Tailwind color families, each bound to a semantic role — zinc = neutral surfaces/text/borders, blue = brand/interactive, emerald = success, amber = warning, red = danger, purple = member accent. Stray off-family usages found by grep (`green-*`, `rose-*`, `violet-*`) are rewritten onto the canonical scale (`bg-green-600` → `bg-emerald-600`, `bg-rose-500` → `bg-zinc-400`) rather than introducing new scales. Semantic status colors stay recognizable when the rest of the palette changes, so staff don't have to relearn them. Confidence: 0.7
- Contrast targets are deliberately ASYMMETRIC between modes ("Tôi muốn nó ít contrast ở dark và nhiều contrast ở light"): LIGHT mode maximizes contrast — pure white page background, near-black headings, and muted/secondary/tertiary text pushed up to clear WCAG AA (secondary ~10:1, muted ~4.8:1); DARK mode minimizes contrast to be easy on the eyes — no pure-black page (`#15181d`), ivory/softened body text instead of pure white (~14.5:1 rather than ~17–19:1), lower-contrast muted text (~5.7:1), softer borders and lighter shadows. Confidence: 0.8
- Reject warm/yellow-tinted ("ám vàng") neutrals — cream/paper backgrounds, stone/amber-shifted grays, warm charcoal. Neutral surfaces, borders and text must be cool/cool-gray (slate-like, e.g. `#f8fafc`, `#ffffff`, `#15181d`, `#e6e8ec`). Gold stays only as a small brand accent (wordmark/tagline), never as the cast of the base palette. Confidence: 0.8
- Don't invent a new brand palette for an existing UI: when asked to re-theme, keep the identity color families at their stock Tailwind default values (`blue` brand `#2563eb`, `emerald`/`amber`/`red`/`purple` semantics) and confine changes to the neutral scale; the user asked to "loại bỏ palette … tôi muốn màu cũ" and had the redesign (navy `#2a5290` brand + warm palette) fully reverted back to the original blue. Semantic status colors must not be recolored at all. Confidence: 0.75
- Nested modals must not interfere with their parent: in the shared `Modal`, body-scroll lock is reference-counted (a module-level `openModalCount` — closing a child modal only restores `document.body.style.overflow` when the count reaches 0, so it can't unlock the still-open parent), and Escape closes ONLY the topmost modal — determined by DOM order via a `data-modal-panel` marker on the content div (`querySelectorAll("[data-modal-panel]")`, compare the last one against `contentRef.current`) rather than a module-level id stack or per-instance ref bookkeeping, which tripped the project's react-hooks lint rules. Apply the fix in the base component so every stacked modal inherits it. Confidence: 0.7
- For list/listing screens that summarize data and link out to a dedicated detail screen (e.g., the shifts list page → per-shift transactions page), keep the list view minimal — show only counts and identifiers (`N giao dịch · M phiên`, status badge, time range, owner) — and move the full reconciliation/inventory/cash breakdown to the detail page. Don't replicate detailed reconciliation data on the list view AND in the detail view. The user explicitly trimmed the shifts list to counts only ("Tôi không cần hiển thị đối soát ca trong báo cáo chỉ cần hiển thị số lượng ca là được chuyển đối soát vào xem giao dịch trong ca") because the list's job is scanning/filtering, not reconciling. Pair this with: make the whole row itself a `<Link>` to the detail page rather than a separate action button — clicking anywhere on the row navigates, and any secondary action (e.g., a "NV" management button for admins) must `event.preventDefault()` + `event.stopPropagation()` so it doesn't double-fire the navigation. Confidence: 0.85
